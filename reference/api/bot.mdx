---
title: "Bot Identity API"
description: "Bot Identity endpoints — generated from the octo-cli OpenAPI spec."
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/bot.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> Generated from [`octo-cli/internal/registry/specs/bot.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/bot.json) — **6 operations**.

### Register (authenticate) the bot and obtain IM token + URLs

`POST /v1/bot/register` · CLI op: `bot.register` · 🟠 write

Routes by token prefix: app_* → App Bot, bf_* → User Bot.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `agent_platform` | string |  |  |
| `agent_version` | string |  |  |
| `plugin_version` | string |  |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Bot identity + connection info |

---

### Publish the bot's command list

`POST /v1/bot/setCommands` · CLI op: `bot.set-commands` · 🟠 write

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `commands` | object[] | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Saved |

---

### Look up a user by UID

`GET /v1/bot/user/info` · CLI op: `bot.user-info` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `uid` | query | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | User info |

---

### List members of the bot's space

`GET /v1/bot/space/members` · CLI op: `bot.space-members` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `keyword` | query | string |  | optional LIKE filter on name |
| `space_id` | query | string |  | override space (defaults to bot's space) |
| `limit` | query | integer |  |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Members |

---

### Send a typing indicator (throttled server-side)

`POST /v1/bot/typing` · CLI op: `bot.typing` · 🟠 write

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `channel_id` | string | ✓ |  |
| `channel_type` | integer | ✓ |  |
| `on_behalf_of` | string |  | OBO: send the typing indicator as this user persona |

**Responses**

| Status | Description |
|---|---|
| `200` | Typing sent (or throttled) |

---

### Report liveness (60s TTL in Redis)

`POST /v1/bot/heartbeat` · CLI op: `bot.heartbeat` · 🟠 write

**Responses**

| Status | Description |
|---|---|
| `200` | Heartbeat recorded |

