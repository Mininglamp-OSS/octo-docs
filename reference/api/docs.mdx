---
title: "Docs (collaborative) API"
description: "Docs (collaborative) endpoints — generated from the octo-cli OpenAPI spec."
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/docs.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> Generated from [`octo-cli/internal/registry/specs/docs.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/docs.json) — **31 operations**.

Call these with a bot token (`bf_…`) as a bearer credential, or via [`octo-cli`](/guides/bot-developers/drive-octo-with-cli). See the [API overview](/reference/rest-websocket-api) for authentication and risk levels.

### List documents the bot owns or is a member of (page-based)

`GET /v1/bot/docs` · CLI op: `docs.list` · 🟢 read

Page-based listing scoped to the bot's server-resolved space. Response is &#123;total, items&#125;; there is no cursor, so --page-all does not apply — walk pages with --page/--pageSize.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `folderId` | query | string |  | filter to a single folder |
| `page` | query | integer |  | 1-based page number |
| `pageSize` | query | integer |  | items per page (1-100, default 20) |
| `sort` | query | enum(`updatedAt:desc`, `updatedAt:asc`) |  | sort order by last update |

**Responses**

| Status | Description |
|---|---|
| `200` | A page of documents |

---

### Create an (empty) document; the caller becomes owner/admin

`POST /v1/bot/docs` · CLI op: `docs.create` · 🟠 write

Create takes no body content — a new doc starts empty and its body is edited over the Yjs websocket, not REST.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `title` | string |  | initial title (optional; defaults to empty) |
| `folderId` | string |  | target folder id (optional; defaults to the space default folder) |
| `docType` | string |  | document type (optional; defaults to 'doc') |

**Responses**

| Status | Description |
|---|---|
| `201` | Created document metadata |

---

### Fetch one document's metadata (needs reader)

`GET /v1/bot/docs/{docId}` · CLI op: `docs.get` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Document metadata |

---

### Rename a document (needs admin)

`PATCH /v1/bot/docs/{docId}` · CLI op: `docs.rename` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `title` | string | ✓ | new non-empty title |

**Responses**

| Status | Description |
|---|---|
| `200` | Renamed |

---

### Soft-delete a document (needs admin)

`DELETE /v1/bot/docs/{docId}` · CLI op: `docs.delete` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Deleted |

---

### Export a whiteboard's live scene as a PNG or SVG image (needs reader)

`GET /v1/bot/docs/{docId}/export` · CLI op: `docs.scene.export` · 🟢 read

Renders the LIVE Excalidraw scene of a doc_type 'board' to an image on the server: `--image-format png` (default) returns a PNG, `--image-format svg` returns an SVG. The body is binary, so pass --output/-o to save it to a file (e.g. `docs scene export &lt;docId&gt; --image-format png -o board.png`); without --output the command only reports the response status, content-type and size. A non-board target returns 409 unsupported_doc_type; an unrecognised format returns 400 invalid_format.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `format` | query | enum(`png`, `svg`) |  | image format (one of: png, svg); default png. Exposed as --image-format so it does not shadow the global --format output flag. |

**Responses**

| Status | Description |
|---|---|
| `200` | The rendered board image |
| `400` | invalid_format |
| `409` | unsupported_doc_type / board_snapshot_invalid |

---

### Read a document's live body + its base version (needs reader)

`GET /v1/bot/docs/{docId}/content` · CLI op: `docs.content.get` · 🟢 read

Reads the LIVE ProseMirror body of a doc_type 'doc' plus the base-version token that pins the read. Pass that token to `docs content edit` as --base-version so the optimistic-concurrency guard can prove the body did not change between read and write. board / whiteboard bodies are a different Y.Doc shape and return 409 unsupported_doc_type here.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Live body + base version |

---

### Apply an incremental block-op edit to a document body (needs writer)

`PATCH /v1/bot/docs/{docId}/content` · CLI op: `docs.content.edit` · 🟠 write

Applies a batch of incremental block ops to a doc_type 'doc' body under a mandatory optimistic-concurrency guard. Pass the base-version token from `docs content get` via --base-version (sent as the If-Match header); the server rejects with 412 base_version_stale if the live body moved since that read. The ops batch is a structured array, so pass it as JSON through --data. Only doc_type 'doc' is accepted (board / whiteboard return 409 unsupported_doc_type). Gates: op count over the max → 413 too_many_ops, a single op's content too large → 413 op_content_too_large, block-path too deep → 400 path_too_deep, missing base version or malformed shape → 400 invalid_body, bad anchor / invalid ops → 422.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `If-Match` | header | string | ✓ | base-version token from 'docs content get' (sent as the If-Match header for optimistic concurrency; required) |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `ops` | object[] | ✓ | ordered batch of incremental block edit ops. Each op is one of: insert &#123;"type":"insert","at":&#123;"path":[i,...],"position":"before\|after\|inside_start\|inside_end"&#125;,"content":[&lt;ProseMirror block nodes&gt;],"expect":&#123;"type":"&lt;nodeType&gt;"&#125;?&#125;; replace &#123;"type":"replace","range":&#123;"from":&#123;"path":[...]&#125;,"to":&#123;"path":[...]&#125;&#125;,"content":[...],"expect":?&#125;; delete &#123;"type":"delete","range":&#123;"from":&#123;"path":[...]&#125;,"to":&#123;"path":[...]&#125;&#125;,"expect":?&#125;. `path` is child indices from the doc root; range endpoints must share a parent. Pass via --data. |

**Responses**

| Status | Description |
|---|---|
| `200` | Edit applied; carries the new base version |

---

### Read a spreadsheet's live cells + dims + base version (needs reader)

`GET /v1/bot/docs/{docId}/sheet` · CLI op: `docs.sheet.get` · 🟢 read

Reads the LIVE flat cell map of a doc_type 'sheet' (keys `sheetId!row:col`, values &#123;v,f,s&#125;), its column-width / row-height overrides (sheetDims), its cell hyperlinks (sheetHyperLinks), and the base-version token that pins the read. Pass that token to `docs sheet edit` as --base-version so the optimistic-concurrency guard can prove the sheet did not change between read and write. doc / board / whiteboard bodies are a different Y.Doc shape and return 409 unsupported_doc_type here. A whole-sheet read of a grid over the 1MB cap returns 413 sheet_too_large; pass --limit (and follow nextCursor via --cursor) to read it in pages instead. In paged mode each page is bounded by both --limit and the byte cap, sheetDims and sheetHyperLinks are returned on the first page only, and the response carries hasMore + nextCursor. A --cursor whose snapshot moved returns 409 sheet_changed (restart from the first page); a bad --cursor returns 400 invalid_cursor; a non-positive --limit returns 400 invalid_limit.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `limit` | query | integer |  | max cells per page; opts into paged reads. Bounded by the server byte cap regardless. Omit for a whole-sheet read. |
| `cursor` | query | string |  | opaque nextCursor from the previous page; also opts into paged reads. A cursor whose snapshot moved returns 409 sheet_changed. |

**Responses**

| Status | Description |
|---|---|
| `200` | Live cells + dims + base version (paged when --limit/--cursor is set) |

---

### Apply a batch sheet edit — cells / dims / drawings / hyperlinks (needs writer)

`PATCH /v1/bot/docs/{docId}/sheet` · CLI op: `docs.sheet.edit` · 🟠 write

Applies a batch of cell, dimension, and/or drawing set/delete ops to a doc_type 'sheet' under a mandatory optimistic-concurrency guard. Pass the base-version token from `docs sheet get` via --base-version (sent as the If-Match header); the server rejects with 412 base_version_stale if the live sheet moved since that read. The cells batch is a JSON object `&#123; "sheetId!row:col": &#123;v,f,s&#125; \| null &#125;` (a null value deletes the cell). The optional dims batch sets column widths / row heights: a JSON object `&#123; "c&lt;idx&gt;"\|"r&lt;idx&gt;": &lt;px&gt; \| null &#125;` (e.g. `&#123;"c0":200,"r3":40&#125;`; a null value deletes that dim). The optional drawings batch inserts/removes floating images: a JSON object `&#123; "sheetId!drawingId": &lt;ISheetImage&gt; \| null &#125;` (image bytes ride inline as a base64 data URL in `source`; a math formula rides here too as a float-DOM drawing with `drawingType:8`, `componentKey:"octo-math-formula"`, and the LaTeX in `data.latex`). The optional hyperlinks batch sets cell links: a JSON object `&#123; "sheetId!linkId": &#123;id,row,column,payload,display?&#125; \| null &#125;` (payload is the URL — http/https/mailto or an internal #jump — and the link points at a cell by row/column; put the visible text in that cell's `v`). Provide `cells`, `dims`, `drawings`, or `hyperlinks` via --data; at least one must be non-empty. Only doc_type 'sheet' is accepted (doc / board / whiteboard return 409 unsupported_doc_type). Gates: cell/dim/drawing count over the max → 413 too_many_cells, a single cell's payload too large → 413 cell_too_large, missing base version / malformed shape / all batches empty → 400 invalid_body, a cell/dim/drawing violating its contract or key shape → 422 sheet_cell_invalid.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `If-Match` | header | string | ✓ | base-version token from 'docs sheet get' (sent as the If-Match header for optimistic concurrency; required) |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `cells` | object |  | batch of cell ops keyed by cell key `sheetId!row:col` (e.g. `default!0:0`). Each value is either &#123;"v":&lt;string\|number\|boolean\|null&gt;,"f":"&lt;formula&gt;"?,"s":&lt;style object&gt;?&#125; to set the cell, or null to delete it. At least one of v/f/s must be present on a set. Pass via --data. |
| `dims` | object |  | batch of column-width / row-height ops keyed `c&lt;idx&gt;` (column) or `r&lt;idx&gt;` (row), e.g. `&#123;"c0":200,"r3":40&#125;`. Each value is a positive pixel number to set, or null to delete that dim. Optional; provide cells, dims, or drawings (at least one non-empty). Pass via --data. |
| `drawings` | object |  | batch of floating-image ops keyed `$&#123;sheetId&#125;!$&#123;drawingId&#125;` (e.g. `default!img1`). Each value is a serialized Univer ISheetImage — &#123;drawingId (== the key's id), drawingType:0, imageSourceType:"BASE64", source:"data:image/png;base64,…", transform:&#123;left,top,width,height,angle,flipX,flipY,skewX,skewY&#125;, sheetTransform:&#123;from:&#123;column,row,columnOffset,rowOffset&#125;,to:&#123;…&#125;&#125;, axisAlignSheetTransform&#125; — or null to delete it. The image bytes ride inline in `source`. A MATH FORMULA is written here too, as a float-DOM drawing: &#123;drawingId, drawingType:8, componentKey:"octo-math-formula", data:&#123;latex,id,fontSize&#125;, transform, sheetTransform&#125; (latex in data.latex; data.id == drawingId). Optional. Pass via --data. |
| `hyperlinks` | object |  | batch of cell-hyperlink ops keyed `$&#123;sheetId&#125;!$&#123;linkId&#125;` (e.g. `default!lnk1`). Each value is &#123;id (== the key's linkId), row, column (0-based cell anchor), payload:"&lt;url&gt;" (http/https/mailto or an internal #jump), display?:"&lt;label&gt;"&#125; to set, or null to delete. The link lives outside cell data and points at a cell by row/column; put the cell's visible text in that cell's `v`. Optional; provide cells, dims, drawings, or hyperlinks (at least one non-empty). Pass via --data. |

**Responses**

| Status | Description |
|---|---|
| `200` | Edit applied; carries the new base version |

---

### Read a whiteboard's live scene + its base version (needs reader)

`GET /v1/bot/docs/{docId}/scene` · CLI op: `docs.scene.get` · 🟢 read

Reads the LIVE Excalidraw scene of a doc_type 'board' — its `elements` in fractional-index (z-order), the `files` map of image/file refs, and the base-version token that pins the read. Pass that token to `docs scene edit` as --base-version so the optimistic-concurrency guard can prove the scene did not change between read and write. doc / sheet bodies are a different Y.Doc shape and return 409 unsupported_doc_type here; a live scene that decodes to a wrong-kind / corrupt blob returns 409 board_snapshot_invalid.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Live scene + base version |

---

### Apply an element upsert/delete batch to a whiteboard scene (needs writer)

`PATCH /v1/bot/docs/{docId}/scene` · CLI op: `docs.scene.edit` · 🟠 write

Applies a batch of element upserts and soft-deletes to a doc_type 'board' scene under a mandatory optimistic-concurrency guard. Pass the base-version token from `docs scene get` via --base-version (sent as the If-Match header); the server rejects with 412 base_version_stale if the live scene moved since that read. `elements` are full elements to upsert (CAS: the higher `version` wins), `deletedElementIds` are soft-delete tombstones, and `files` upserts file refs; the batch is a structured object, so pass it as JSON through --data. Every element in `elements` must carry a valid fractional-index `index` (z-order key); the CLI rejects an index-less or malformed-index element locally before sending. Only doc_type 'board' is accepted (doc / sheet return 409 unsupported_doc_type). Gates: element count over the max → 413 too_many_elements, a single element too large → 413 element_too_large, whole scene over the cap → 413 doc_too_large, an element failing the whitelist → 422 board_element_invalid, a file ref with no usable attachId → 422 board_file_invalid, missing base version or malformed shape → 400 invalid_body.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `If-Match` | header | string | ✓ | base-version token from 'docs scene get' (sent as the If-Match header for optimistic concurrency; required) |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `elements` | object[] |  | full Excalidraw elements to upsert; on a key collision the element with the higher `version` (then smaller `versionNonce`) wins the CAS. Each element MUST carry a valid fractional-index `index` (z-order key produced by the fractional-indexing / Excalidraw rules, e.g. `a0`); an index-less or malformed-index element is rejected locally before sending. Pass via --data. |
| `deletedElementIds` | string[] |  | element ids to soft-delete; each becomes a tombstone with a superseding version so it converges under CAS. Pass via --data. |
| `files` | object |  | map of fileId -&gt; file ref (e.g. &#123;"attachId":"…"&#125;) to upsert for images/files referenced by the scene. Pass via --data. |

**Responses**

| Status | Description |
|---|---|
| `200` | Edit applied; carries the new base version |

---

### List a document's members (needs admin)

`GET /v1/bot/docs/{docId}/members` · CLI op: `docs.members.list` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Members |

---

### Add or change a member's role by uid (upsert; needs admin)

`PUT /v1/bot/docs/{docId}/members` · CLI op: `docs.members.set` · 🟠 write

PUT-upsert: adds the member if absent or changes the role if present. The target uid must be a real Octo user or the backend returns 404 user_not_found.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `uid` | string | ✓ | target user uid |
| `role` | enum(`reader`, `writer`, `admin`) | ✓ | role to grant |

**Responses**

| Status | Description |
|---|---|
| `200` | Upserted |

---

### Remove a member (needs admin; the owner cannot be removed)

`DELETE /v1/bot/docs/{docId}/members/{uid}` · CLI op: `docs.members.remove` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `uid` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Removed |

---

### Read a document's space-level share scope (needs reader)

`GET /v1/bot/docs/{docId}/share` · CLI op: `docs.share.get` · 🟢 read

Returns the current share settings any viewer can see: &#123;docId, shareScope, shareRole&#125;. shareScope is 'restricted' (only owner/members) or 'anyone_in_space' (every member of the doc's space gets the share role). shareRole is meaningful only when shareScope is anyone_in_space; a restricted doc always reports shareRole 'read'.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Current share settings |

---

### Set a document's space-level share scope (needs admin)

`PUT /v1/bot/docs/{docId}/share` · CLI op: `docs.share.set` · 🟠 write

Change the share scope so either only owner/members can access (--scope restricted) or every member of the doc's space gets a share role (--scope anyone_in_space --role read\|edit). Normalization matches the backend: with --scope anyone_in_space a valid --role is required (a missing/invalid role is 400 invalid_role); with --scope restricted the role is ignored and stored as read, so --role may be omitted. An unknown scope is 400 invalid_scope; a non-admin caller is 403; a missing/cross-space doc is 404; an archived doc is 409. Share only ever RAISES access (owner/members are never downgraded).

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `shareScope` | enum(`restricted`, `anyone_in_space`) | ✓ | share scope to set |
| `shareRole` | enum(`read`, `edit`) |  | share role for anyone_in_space (required when scope=anyone_in_space; ignored and stored as read when scope=restricted) |

**Responses**

| Status | Description |
|---|---|
| `200` | Updated share settings |
| `400` | invalid_scope / invalid_role |
| `403` | forbidden (not admin/owner) |
| `404` | not_found (missing or cross-space doc) |
| `409` | conflict (archived doc) |

---

### Grant (never downgrade) a single uid reader/writer access (needs admin)

`POST /v1/bot/docs/{docId}/forward-grant` · CLI op: `docs.forward-grant` · 🟠 write

Max-merge grant used when forwarding a doc to a chat recipient. Only reader or writer is grantable here; admin is not forward-grantable.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `uid` | string | ✓ | target user uid |
| `role` | enum(`reader`, `writer`) | ✓ | role to grant (grant-only, never downgrades) |

**Responses**

| Status | Description |
|---|---|
| `200` | Grant result (final role + changed flag) |

---

### List comment thread roots with their replies (needs reader)

`GET /v1/bot/docs/{docId}/comments` · CLI op: `docs.comments.list` · 🟢 read

Returns &#123;items, nextCursor&#125;; there is no &#123;data, pagination&#125; envelope, so --page-all does not apply — pass --cursor with the returned nextCursor to walk pages.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `includeResolved` | query | string |  | set to 1 to include resolved threads |
| `cursor` | query | integer |  | id cursor from a previous page's nextCursor |
| `limit` | query | integer |  | page size (1-100, default 50) |

**Responses**

| Status | Description |
|---|---|
| `200` | A page of comment threads |

---

### Add a root comment (anchored) or a reply (needs reader)

`POST /v1/bot/docs/{docId}/comments` · CLI op: `docs.comments.add` · 🟠 write

A reply sets parentId and omits anchors. A root comment needs an anchor: either the opaque base64 Yjs positions anchorStart/anchorEnd (from a live editor selection), or anchorText — the selected text — which the backend resolves against the document. When anchorText matches more than one place the request fails with 422 ambiguous_anchor; narrow it with blockPath (the block's child-index path, e.g. 0,2) and/or occurrence (1-based match index). For a doc_type 'sheet' anchorText is rejected with 409 unsupported_doc_type (a sheet has no text fragment): anchor to a cell instead by passing the base64 of the cell key `$&#123;sheetId&#125;!$&#123;row&#125;:$&#123;col&#125;` (row/col 0-based, e.g. base64 of `default!0:0`) as BOTH anchorStart and anchorEnd, with the A1 label in anchorText (display only).

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `body` | string | ✓ | comment text |
| `parentId` | integer |  | thread root id to reply to (omit for a root comment) |
| `anchorStart` | string |  | root only: base64-encoded Yjs start position (from a live editor selection) |
| `anchorEnd` | string |  | root only: base64-encoded Yjs end position (from a live editor selection) |
| `anchorText` | string |  | root only: the exact selected text; the backend resolves it to an anchor. Use instead of anchorStart/anchorEnd when you have no live editor positions (e.g. a bot) |
| `blockPath` | string |  | root only: disambiguate anchorText by block, as a comma-separated child-index path from the doc root (e.g. 0,2) |
| `occurrence` | integer |  | root only: disambiguate anchorText by picking the Nth match (1-based) among the remaining candidates |

**Responses**

| Status | Description |
|---|---|
| `201` | Created (returns the new comment id) |

---

### Edit a comment body (author) or resolve/reopen a thread (writer)

`PATCH /v1/bot/docs/{docId}/comments/{id}` · CLI op: `docs.comments.edit` · 🟠 write

Pass --body to edit your own comment's text, or --resolved to resolve/reopen a thread root. Provide exactly one.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `id` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `body` | string |  | new comment text (author only) |
| `resolved` | boolean |  | resolve (true) or reopen (false) a thread root (writer only) |

**Responses**

| Status | Description |
|---|---|
| `200` | Updated |

---

### Soft-delete your own comment, or hard-delete as admin

`DELETE /v1/bot/docs/{docId}/comments/{id}` · CLI op: `docs.comments.delete` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `id` | path | string | ✓ |  |
| `hard` | query | string |  | set to 1 for an admin hard delete (default is a soft delete by the author) |

**Responses**

| Status | Description |
|---|---|
| `200` | Deleted |

---

### List saved versions/snapshots (needs reader)

`GET /v1/bot/docs/{docId}/versions` · CLI op: `docs.versions.list` · 🟢 read

Returns &#123;items, nextCursor, counts&#125;; there is no &#123;data, pagination&#125; envelope, so --page-all does not apply — pass --cursor with the returned nextCursor to walk pages.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `cursor` | query | integer |  | id cursor from a previous page's nextCursor |
| `limit` | query | integer |  | page size |
| `kind` | query | enum(`manual`, `auto`, `all`) |  | filter by snapshot kind |
| `includeAuto` | query | string |  | back-compat alias honoured only when kind is absent; set to 1 to include auto snapshots |

**Responses**

| Status | Description |
|---|---|
| `200` | A page of versions |

---

### Take a named snapshot of the live document (needs writer)

`POST /v1/bot/docs/{docId}/versions` · CLI op: `docs.versions.create` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `label` | string |  | human label for the snapshot (optional) |

**Responses**

| Status | Description |
|---|---|
| `201` | Created (returns docVersionSeq) |

---

### Read a version's decoded content snapshot (read-only preview; needs reader)

`GET /v1/bot/docs/{docId}/versions/{versionId}/state` · CLI op: `docs.versions.state` · 🟢 read

Returns the version decoded to structured content, kind-selected by the doc's doc_type. A document/sheet decodes to ProseMirror JSON (&#123;kind:'document', doc, sheetCells, sheetDims, schemaVersion, docVersionSeq&#125;); a board decodes to an Excalidraw scene (&#123;kind:'board', scene, schemaVersion, docVersionSeq&#125;). This is a read-only preview of a past snapshot, not the live document, and cannot be written.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `versionId` | path | string | ✓ | the version's docVersionSeq (the integer returned by `docs versions create` and shown as `docVersionSeq` in `docs versions list`); there is no separate id field |

**Responses**

| Status | Description |
|---|---|
| `200` | Decoded snapshot content (document/sheet or board scene, selected by kind) |

---

### Rename a saved version (needs writer)

`PATCH /v1/bot/docs/{docId}/versions/{versionId}` · CLI op: `docs.versions.rename` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `versionId` | path | string | ✓ | the version's docVersionSeq (the integer returned by `docs versions create` and shown as `docVersionSeq` in `docs versions list`); there is no separate id field |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `label` | string | ✓ | new non-empty label |

**Responses**

| Status | Description |
|---|---|
| `200` | Renamed |

---

### Delete a saved version (needs admin)

`DELETE /v1/bot/docs/{docId}/versions/{versionId}` · CLI op: `docs.versions.delete` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `versionId` | path | string | ✓ | the version's docVersionSeq (the integer returned by `docs versions create` and shown as `docVersionSeq` in `docs versions list`); there is no separate id field |

**Responses**

| Status | Description |
|---|---|
| `200` | Deleted |

---

### Restore the document to a saved version (needs admin)

`POST /v1/bot/docs/{docId}/versions/{versionId}/restore` · CLI op: `docs.versions.restore` · 🟠 write

Non-destructive forward reconcile of the target version into the live state. A safety snapshot of the pre-restore state is recorded first, so a restore is itself undoable.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `versionId` | path | string | ✓ | the version's docVersionSeq (the integer returned by `docs versions create` and shown as `docVersionSeq` in `docs versions list`); there is no separate id field |

**Responses**

| Status | Description |
|---|---|
| `200` | Restored (returns restoredFrom + newDocVersionSeq) |

---

### Register an attachment and get a presigned upload URL (needs writer)

`POST /v1/bot/docs/{docId}/attachments/presign` · CLI op: `docs.attachments.presign` · 🟠 write

Metadata-only. Returns a presigned PUT URL + headers; the CLI does not upload the bytes. After presign, PUT the file directly to uploadUrl with the returned headers (see the octo-docs skill).

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `fileName` | string | ✓ | original file name |
| `mime` | string | ✓ | MIME type (must pass the backend allow/deny list) |
| `sizeBytes` | integer | ✓ | file size in bytes (must be within the tier cap) |

**Responses**

| Status | Description |
|---|---|
| `200` | Presigned upload target |

---

### Get a freshly signed, time-limited download URL for one attachment (needs reader)

`GET /v1/bot/docs/{docId}/attachments/{attachId}` · CLI op: `docs.attachments.get` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |
| `attachId` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Signed download URL + metadata |

---

### Batch-resolve attachIds to freshly signed download URLs (needs reader)

`POST /v1/bot/docs/{docId}/attachments/resolve` · CLI op: `docs.attachments.resolve` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `docId` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `attachIds` | string[] | ✓ | attachment ids to resolve (non-empty; capped by the backend batch limit) |

**Responses**

| Status | Description |
|---|---|
| `200` | Signed URLs + not-found ids |

