---
title: "Docs (interactive HTML) API"
description: "Docs (interactive HTML) endpoints — generated from the octo-cli OpenAPI spec."
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/html.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> Generated from [`octo-cli/internal/registry/specs/html.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/html.json) — **20 operations**.

Call these with an Assistant token (`bf_…`) as a bearer credential, or via [`octo-cli`](/guides/bot-developers/drive-octo-with-cli). See the [API overview](/reference/rest-websocket-api) for authentication and risk levels.

### List documents (owner-scoped index)

`GET /v1/docs` · CLI op: `html.list` · 🟢 read

Owner-scoped document index. The owner check is enforced server-side; a non-owner session is rejected 403.

**Responses**

| Status | Description |
|---|---|
| `200` | A list of documents |

---

### Publish an HTML document (creates or bumps a version)

`POST /v1/docs` · CLI op: `html.publish` · 🟠 write

Publish a self-contained HTML document under a slug. Requires the write token (Bearer). A new slug creates the document; an existing slug appends a new immutable version. The server stamps commentable artifacts and reconciles existing comment anchors.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `slug` | string | ✓ | stable document slug |
| `html` | string | ✓ | the full self-contained HTML document body |
| `version` | integer |  | optional explicit version; omit to auto-increment |
| `meta` | object |  | optional metadata (e.g. &#123;"title":"..."&#125;) |
| `mount_type` | enum(`group`, `space`, `thread`) |  | where this doc is mounted; forwarded to docs-backend so the HTML doc is registered into the file sidebar. Omit to skip registration (default). 'group'/'space' register; 'thread' is accepted but not registered (thread mounts are intentionally skipped by the backend). |
| `group_no` | string |  | group number; supply when mount_type=group |
| `thread_id` | string |  | thread id when mount_type=thread (mount context only; thread mounts are not registered) |

**Responses**

| Status | Description |
|---|---|
| `200` | Publish result |

---

### Fetch one document's metadata (needs reader)

`GET /v1/docs/{slug}` · CLI op: `html.get` · 🟢 read

Return a single document's metadata (slug, title, latest version, timestamps). Backend endpoint GET /v1/docs/&#123;slug&#125; — added to octo-doc to back this command.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Document metadata |

---

### Delete a document (author-only, soft delete)

`DELETE /v1/docs/{slug}` · CLI op: `html.rm` · 🟠 write

Soft-delete a document. Requires the write token / doc author credential.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Deleted |

---

### List a document's versions (needs reader)

`GET /v1/docs/{slug}/versions` · CLI op: `html.versions` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Version list |

---

### Save the author draft (author-only)

`PUT /v1/docs/{slug}/draft` · CLI op: `html.draft.save` · 🟠 write

Write the author-only draft slot for a slug. Does not mint an immutable version until promoted.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `html` | string | ✓ | the draft HTML body |

**Responses**

| Status | Description |
|---|---|
| `200` | Draft saved |

---

### Promote the author draft to an immutable version (author-only)

`POST /v1/docs/{slug}/draft/promote` · CLI op: `html.draft.promote` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Promoted |

---

### Mint / rotate a per-doc read+comment share code (author-only)

`POST /v1/docs/{slug}/share` · CLI op: `html.share` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Share code |

---

### Revoke the per-doc share code (author-only)

`DELETE /v1/docs/{slug}/share` · CLI op: `html.unshare` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Revoked |

---

### List a document's per-uid access grants (author-only)

`GET /v1/docs/{slug}/grants` · CLI op: `html.grant.list` · 🟢 read

List the uids granted access to this document and their role. Author-only (only the doc creator can view grants).

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Grant list |

---

### Grant a uid access to a document (author-only)

`PUT /v1/docs/{slug}/grants` · CLI op: `html.grant.add` · 🟠 write

Grant a specific uid access to this document (upsert by uid). role defaults to 'reader' (read published versions + comment/react). Only the doc creator (author) can grant. The granted uid resolves to a reader capability on subsequent requests — no share code needed.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `uid` | string | ✓ | the uid to grant access to |
| `role` | enum(`reader`) |  | capability to grant; defaults to 'reader' when omitted |

**Responses**

| Status | Description |
|---|---|
| `200` | Grant added |

---

### Revoke a uid's access grant (author-only)

`DELETE /v1/docs/{slug}/grants/{uid}` · CLI op: `html.grant.rm` · 🟠 write

Revoke a uid's grant on this document. Author-only. The doc creator cannot be removed (they own the doc) — attempting to returns a conflict.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |
| `uid` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Grant revoked |

---

### List a document's media assets (needs reader)

`GET /v1/docs/{slug}/assets` · CLI op: `html.asset.ls` · 🟢 read

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Asset list |

---

### Upload a media asset to a document (author-only)

`POST /v1/docs/{slug}/assets` · CLI op: `html.asset.add` · 🟠 write

Upload a media asset for a document via multipart/form-data (form field "file"). The asset is content-addressed (sha256); requires the write token / doc author credential.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Asset registered |

---

### Delete a media asset (author-only)

`DELETE /v1/docs/{slug}/assets/{sha256}` · CLI op: `html.asset.rm` · 🟠 write

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | path | string | ✓ |  |
| `sha256` | path | string | ✓ |  |

**Responses**

| Status | Description |
|---|---|
| `200` | Deleted |

---

### List a document's comments (needs reader)

`GET /v1/comments` · CLI op: `html.comment.list` · 🟢 read

List comments for a document. The slug is passed as a query parameter.

**Parameters**

| Name | In | Type | Required | Description |
|---|---|---|---|---|
| `slug` | query | string | ✓ |  |
| `version` | query | string |  | version to fold to, or 'all' |

**Responses**

| Status | Description |
|---|---|
| `200` | Comment list |

---

### Create a comment on a document (needs reader capability)

`POST /v1/comments` · CLI op: `html.comment.add` · 🟠 write

Create a comment anchored to text or an artifact aid. Requires at least a reader capability (the doc's share code) or the write token.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `slug` | string | ✓ |  |
| `text` | string | ✓ | comment body |
| `anchor` | object |  | optional anchor (text or element by aid); omit for an unanchored comment |

**Responses**

| Status | Description |
|---|---|
| `200` | Comment created |

---

### Post an agent reply + verdict to a comment (write-token gated)

`POST /v1/agent/replies` · CLI op: `html.reply` · 🟠 write

Reply to a comment thread as the agent (identity odoc-agent) with an optional verdict: applied / partial / question. Requires the write token.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `slug` | string | ✓ |  |
| `parent_id` | string | ✓ | the comment thread root id being replied to |
| `text` | string | ✓ | reply body |
| `status` | enum(`applied`, `partial`, `question`) |  | optional verdict rendered on the parent |
| `applied_in` | integer |  | optional version the change was applied in |

**Responses**

| Status | Description |
|---|---|
| `200` | Reply posted |

---

### Read one artifact's outer HTML by aid (write-token gated)

`POST /v1/agent/element/get` · CLI op: `html.element.get` · 🟢 read

Return the outer HTML of a single artifact located by its stamped data-odoc-aid in a document version (version 0 = latest). Requires the write token.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `slug` | string | ✓ |  |
| `version` | integer |  | version to read; 0 or omitted = latest |
| `aid` | string | ✓ | the artifact's stamped data-odoc-aid |

**Responses**

| Status | Description |
|---|---|
| `200` | Element outer HTML |

---

### Replace one artifact's outer HTML by aid and republish (write-token gated)

`POST /v1/agent/element/replace` · CLI op: `html.element.replace` · 🟠 write

Swap one artifact's outer HTML (located by aid in base_version, 0 = latest) with new_html and republish a new version. new_html must be exactly one top-level element and free of scripts / inline event handlers / javascript: URLs. Requires the write token.

**Request body**

| Field | Type | Required | Description |
|---|---|---|---|
| `slug` | string | ✓ |  |
| `base_version` | integer |  | version whose aid to locate; 0 or omitted = latest |
| `aid` | string | ✓ | the artifact's stamped data-odoc-aid |
| `new_html` | string | ✓ | replacement single top-level element |

**Responses**

| Status | Description |
|---|---|
| `200` | Replaced + republished |

