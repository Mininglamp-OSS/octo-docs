---
title: "octo-cli event"
description: "octo-cli commands in the event domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/event.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 2 commands, generated from [`octo-cli/internal/registry/specs/event.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/event.json). Every invocation emits a structured JSON envelope on stdout.

Authenticate by exporting `OCTO_BOT_TOKEN` (`bf_…`) and `OCTO_API_BASE_URL`. See the [CLI overview](/reference/octo-cli) for common flags and the output contract.

### `event ack`

Acknowledge (remove) an event from the queue · 🟠 write

```bash
octo-cli event ack <event_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<event_id>` | path parameter |

---

### `event list`

Poll pending events (server-side cursor by event_id) · 🟢 read

```bash
octo-cli event list [flags]
```

Uses POST despite being a read — event_id cursor is carried in the request body.

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--event_id` | integer |  | exclusive lower bound; 0 for initial poll |
| `--limit` | integer |  |  |

