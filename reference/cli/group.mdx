---
title: "octo-cli group"
description: "octo-cli commands in the group domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/group.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 9 commands, Generated from [`octo-cli/internal/registry/specs/group.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/group.json). Every invocation emits a structured JSON envelope on stdout.

### `group create`

Create a new group (User Bot only; App Bot rejected) · 🟠 write

```bash
octo-cli group create [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--name` | string |  |  |
| `--data` | json | ✓ | members: JSON payload |
| `--creator` | string |  |  |
| `--space_id` | string |  |  |

---

### `group get`

Get group info · 🟢 read

```bash
octo-cli group get <group_no>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

---

### `group list`

List groups the bot is a member of · 🟢 read

```bash
octo-cli group list [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--space_id` | string |  | optional filter by space |

---

### `group md-get`

Read GROUP.md content · 🟢 read

```bash
octo-cli group md-get <group_no>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

---

### `group md-update`

Update GROUP.md content (bot must be bot_admin) · 🟠 write

```bash
octo-cli group md-update <group_no> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--content` | string | ✓ |  |

---

### `group member-add`

Add members to group (App Bot rejected) · 🟠 write

```bash
octo-cli group member-add <group_no> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--data` | json | ✓ | members: JSON payload |

---

### `group member-remove`

Remove members from group (bot must be bot_admin; App Bot rejected) · 🟠 write

```bash
octo-cli group member-remove <group_no> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--data` | json | ✓ | members: JSON payload |

---

### `group members`

List group members · 🟢 read

```bash
octo-cli group members <group_no>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

---

### `group update`

Update group name/notice (bot must be bot_admin; App Bot rejected) · 🟠 write

```bash
octo-cli group update <group_no> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--name` | string |  |  |
| `--notice` | string |  |  |

