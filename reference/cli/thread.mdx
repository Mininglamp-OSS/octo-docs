---
title: "octo-cli thread"
description: "octo-cli commands in the thread domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/thread.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 8 commands, Generated from [`octo-cli/internal/registry/specs/thread.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/thread.json). Every invocation emits a structured JSON envelope on stdout.

### `thread create`

Create a thread in a group (User Bot only) · 🟠 write

```bash
octo-cli thread create <group_no> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--name` | string | ✓ |  |
| `--source_message_id` | integer |  |  |

---

### `thread get`

Get thread detail (User Bot only) · 🟢 read

```bash
octo-cli thread get <group_no> <short_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |
| `<short_id>` | path parameter |

---

### `thread join`

Bot joins a thread (User Bot only) · 🟠 write

```bash
octo-cli thread join <group_no> <short_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |
| `<short_id>` | path parameter |

---

### `thread leave`

Bot leaves a thread (User Bot only) · 🟠 write

```bash
octo-cli thread leave <group_no> <short_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |
| `<short_id>` | path parameter |

---

### `thread list`

List threads in a group (User Bot only) · 🟢 read

```bash
octo-cli thread list <group_no> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--page_index` | integer |  |  |
| `--page_size` | integer |  |  |

---

### `thread md-get`

Read thread GROUP.md (User Bot only) · 🟢 read

```bash
octo-cli thread md-get <group_no> <short_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |
| `<short_id>` | path parameter |

---

### `thread md-update`

Update thread GROUP.md (User Bot only; must be bot_admin) · 🟠 write

```bash
octo-cli thread md-update <group_no> <short_id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |
| `<short_id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--content` | string | ✓ |  |

---

### `thread members`

List thread members (User Bot only) · 🟢 read

```bash
octo-cli thread members <group_no> <short_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<group_no>` | path parameter |
| `<short_id>` | path parameter |

