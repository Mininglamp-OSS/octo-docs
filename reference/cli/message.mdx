---
title: "octo-cli message"
description: "octo-cli commands in the message domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/message.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 4 commands, Generated from [`octo-cli/internal/registry/specs/message.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/message.json). Every invocation emits a structured JSON envelope on stdout.

### `message edit`

Edit a previously-sent bot message · 🟠 write

```bash
octo-cli message edit [flags]
```

Only the original sender bot may edit. App Bot: DM only.

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--message_id` | string | ✓ |  |
| `--message_seq` | integer |  |  |
| `--channel_id` | string | ✓ |  |
| `--channel_type` | integer | ✓ |  |
| `--content_edit` | string | ✓ |  |

---

### `message read-receipt`

Mark messages as read · 🟠 write

```bash
octo-cli message read-receipt [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | integer |  |  |
| `--data` | json | ✓ | message_ids: JSON payload |

---

### `message send`

Send a message to a channel · 🟠 write

```bash
octo-cli message send [flags]
```

App Bot: DM-only (channel_type=1) and requires friend relationship. User Bot: DM + group + thread (requires membership).

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | integer | ✓ | 1=DM, 2=group, 5=thread |
| `--stream_no` | string |  | optional stream session identifier |
| `--on_behalf_of` | string |  | OBO: send as this user persona; requires an active OBO grant + channel scope |
| `--data` | json | ✓ | payload: message payload (content, type, mention, etc.). Pass via --data (object, not a flag). |

---

### `message sync`

Sync channel messages (pull) · 🟢 read

```bash
octo-cli message sync [flags]
```

App Bot: DM only. User Bot: DM + group + thread (membership required).

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | integer | ✓ |  |
| `--start_message_seq` | integer |  |  |
| `--end_message_seq` | integer |  |  |
| `--limit` | integer |  |  |
| `--pull_mode` | integer |  |  |

