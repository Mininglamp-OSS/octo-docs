---
title: "octo-cli docs"
description: "octo-cli commands in the docs domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/docs.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 31 commands, generated from [`octo-cli/internal/registry/specs/docs.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/docs.json). Every invocation emits a structured JSON envelope on stdout.

Authenticate by exporting `OCTO_BOT_TOKEN` (`bf_…`) and `OCTO_API_BASE_URL`. See the [CLI overview](/reference/octo-cli) for common flags and the output contract.

### `docs attachments get`

Get a freshly signed, time-limited download URL for one attachment (needs reader) · 🟢 read

```bash
octo-cli docs attachments get <docId> <attachId>
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<attachId>` | path parameter |

---

### `docs attachments presign`

Register an attachment and get a presigned upload URL (needs writer) · 🟠 write

```bash
octo-cli docs attachments presign <docId> [flags]
```

Metadata-only. Returns a presigned PUT URL + headers; the CLI does not upload the bytes. After presign, PUT the file directly to uploadUrl with the returned headers (see the octo-docs skill).

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--fileName` | string | ✓ | original file name |
| `--mime` | string | ✓ | MIME type (must pass the backend allow/deny list) |
| `--sizeBytes` | integer | ✓ | file size in bytes (must be within the tier cap) |

---

### `docs attachments resolve`

Batch-resolve attachIds to freshly signed download URLs (needs reader) · 🟢 read

```bash
octo-cli docs attachments resolve <docId> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--data` | json | ✓ | attachIds: attachment ids to resolve (non-empty; capped by the backend batch limit) |

---

### `docs comments add`

Add a root comment (anchored) or a reply (needs reader) · 🟠 write

```bash
octo-cli docs comments add <docId> [flags]
```

A reply sets parentId and omits anchors. A root comment needs an anchor: either the opaque base64 Yjs positions anchorStart/anchorEnd (from a live editor selection), or anchorText — the selected text — which the backend resolves against the document. When anchorText matches more than one place the request fails with 422 ambiguous_anchor; narrow it with blockPath (the block's child-index path, e.g. 0,2) and/or occurrence (1-based match index). For a doc_type 'sheet' anchorText is rejected with 409 unsupported_doc_type (a sheet has no text fragment): anchor to a cell instead by passing the base64 of the cell key `$&#123;sheetId&#125;!$&#123;row&#125;:$&#123;col&#125;` (row/col 0-based, e.g. base64 of `default!0:0`) as BOTH anchorStart and anchorEnd, with the A1 label in anchorText (display only).

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--body` | string | ✓ | comment text |
| `--parentId` | integer |  | thread root id to reply to (omit for a root comment) |
| `--anchorStart` | string |  | root only: base64-encoded Yjs start position (from a live editor selection) |
| `--anchorEnd` | string |  | root only: base64-encoded Yjs end position (from a live editor selection) |
| `--anchorText` | string |  | root only: the exact selected text; the backend resolves it to an anchor. Use instead of anchorStart/anchorEnd when you have no live editor positions (e.g. a bot) |
| `--blockPath` | string |  | root only: disambiguate anchorText by block, as a comma-separated child-index path from the doc root (e.g. 0,2) |
| `--occurrence` | integer |  | root only: disambiguate anchorText by picking the Nth match (1-based) among the remaining candidates |

---

### `docs comments delete`

Soft-delete your own comment, or hard-delete as admin · 🟠 write

```bash
octo-cli docs comments delete <docId> <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--hard` | string |  | set to 1 for an admin hard delete (default is a soft delete by the author) |

---

### `docs comments edit`

Edit a comment body (author) or resolve/reopen a thread (writer) · 🟠 write

```bash
octo-cli docs comments edit <docId> <id> [flags]
```

Pass --body to edit your own comment's text, or --resolved to resolve/reopen a thread root. Provide exactly one.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--body` | string |  | new comment text (author only) |
| `--resolved` | boolean |  | resolve (true) or reopen (false) a thread root (writer only) |

---

### `docs comments list`

List comment thread roots with their replies (needs reader) · 🟢 read

```bash
octo-cli docs comments list <docId> [flags]
```

Returns &#123;items, nextCursor&#125;; there is no &#123;data, pagination&#125; envelope, so --page-all does not apply — pass --cursor with the returned nextCursor to walk pages.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--includeResolved` | string |  | set to 1 to include resolved threads |
| `--cursor` | integer |  | id cursor from a previous page's nextCursor |
| `--limit` | integer |  | page size (1-100, default 50) |

---

### `docs content edit`

Apply an incremental block-op edit to a document body (needs writer) · 🟠 write

```bash
octo-cli docs content edit <docId> [flags]
```

Applies a batch of incremental block ops to a doc_type 'doc' body under a mandatory optimistic-concurrency guard. Pass the base-version token from `docs content get` via --base-version (sent as the If-Match header); the server rejects with 412 base_version_stale if the live body moved since that read. The ops batch is a structured array, so pass it as JSON through --data. Only doc_type 'doc' is accepted (board / whiteboard return 409 unsupported_doc_type). Gates: op count over the max → 413 too_many_ops, a single op's content too large → 413 op_content_too_large, block-path too deep → 400 path_too_deep, missing base version or malformed shape → 400 invalid_body, bad anchor / invalid ops → 422.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--base-version` | string | ✓ | base-version token from 'docs content get' (sent as the If-Match header for optimistic concurrency; required) |
| `--data` | json | ✓ | ops: ordered batch of incremental block edit ops. Each op is one of: insert &#123;"type":"insert","at":&#123;"path":[i,...],"position":"before\|after\|inside_start\|inside_end"&#125;,"content":[&lt;ProseMirror block nodes&gt;],"expect":&#123;"type":"&lt;nodeType&gt;"&#125;?&#125;; replace &#123;"type":"replace","range":&#123;"from":&#123;"path":[...]&#125;,"to":&#123;"path":[...]&#125;&#125;,"content":[...],"expect":?&#125;; delete &#123;"type":"delete","range":&#123;"from":&#123;"path":[...]&#125;,"to":&#123;"path":[...]&#125;&#125;,"expect":?&#125;. `path` is child indices from the doc root; range endpoints must share a parent. Pass via --data. |

---

### `docs content get`

Read a document's live body + its base version (needs reader) · 🟢 read

```bash
octo-cli docs content get <docId>
```

Reads the LIVE ProseMirror body of a doc_type 'doc' plus the base-version token that pins the read. Pass that token to `docs content edit` as --base-version so the optimistic-concurrency guard can prove the body did not change between read and write. board / whiteboard bodies are a different Y.Doc shape and return 409 unsupported_doc_type here.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

---

### `docs create`

Create an (empty) document; the caller becomes owner/admin · 🟠 write

```bash
octo-cli docs create [flags]
```

Create takes no body content — a new doc starts empty and its body is edited over the Yjs websocket, not REST.

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--title` | string |  | initial title (optional; defaults to empty) |
| `--folderId` | string |  | target folder id (optional; defaults to the space default folder) |
| `--docType` | string |  | document type (optional; defaults to 'doc') |

---

### `docs delete`

Soft-delete a document (needs admin) · 🟠 write

```bash
octo-cli docs delete <docId>
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

---

### `docs forward-grant`

Grant (never downgrade) a single uid reader/writer access (needs admin) · 🟠 write

```bash
octo-cli docs forward-grant <docId> [flags]
```

Max-merge grant used when forwarding a doc to a chat recipient. Only reader or writer is grantable here; admin is not forward-grantable.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--uid` | string | ✓ | target user uid |
| `--role` | enum(`reader`, `writer`) | ✓ | role to grant (grant-only, never downgrades) |

---

### `docs get`

Fetch one document's metadata (needs reader) · 🟢 read

```bash
octo-cli docs get <docId>
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

---

### `docs list`

List documents the bot owns or is a member of (page-based) · 🟢 read

```bash
octo-cli docs list [flags]
```

Page-based listing scoped to the bot's server-resolved space. Response is &#123;total, items&#125;; there is no cursor, so --page-all does not apply — walk pages with --page/--pageSize.

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--folderId` | string |  | filter to a single folder |
| `--page` | integer |  | 1-based page number |
| `--pageSize` | integer |  | items per page (1-100, default 20) |
| `--sort` | enum(`updatedAt:desc`, `updatedAt:asc`) |  | sort order by last update |

---

### `docs members list`

List a document's members (needs admin) · 🟢 read

```bash
octo-cli docs members list <docId>
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

---

### `docs members remove`

Remove a member (needs admin; the owner cannot be removed) · 🟠 write

```bash
octo-cli docs members remove <docId> <uid>
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<uid>` | path parameter |

---

### `docs members set`

Add or change a member's role by uid (upsert; needs admin) · 🟠 write

```bash
octo-cli docs members set <docId> [flags]
```

PUT-upsert: adds the member if absent or changes the role if present. The target uid must be a real Octo user or the backend returns 404 user_not_found.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--uid` | string | ✓ | target user uid |
| `--role` | enum(`reader`, `writer`, `admin`) | ✓ | role to grant |

---

### `docs rename`

Rename a document (needs admin) · 🟠 write

```bash
octo-cli docs rename <docId> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--title` | string | ✓ | new non-empty title |

---

### `docs scene edit`

Apply an element upsert/delete batch to a whiteboard scene (needs writer) · 🟠 write

```bash
octo-cli docs scene edit <docId> [flags]
```

Applies a batch of element upserts and soft-deletes to a doc_type 'board' scene under a mandatory optimistic-concurrency guard. Pass the base-version token from `docs scene get` via --base-version (sent as the If-Match header); the server rejects with 412 base_version_stale if the live scene moved since that read. `elements` are full elements to upsert (CAS: the higher `version` wins), `deletedElementIds` are soft-delete tombstones, and `files` upserts file refs; the batch is a structured object, so pass it as JSON through --data. Every element in `elements` must carry a valid fractional-index `index` (z-order key); the CLI rejects an index-less or malformed-index element locally before sending. Only doc_type 'board' is accepted (doc / sheet return 409 unsupported_doc_type). Gates: element count over the max → 413 too_many_elements, a single element too large → 413 element_too_large, whole scene over the cap → 413 doc_too_large, an element failing the whitelist → 422 board_element_invalid, a file ref with no usable attachId → 422 board_file_invalid, missing base version or malformed shape → 400 invalid_body.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--base-version` | string | ✓ | base-version token from 'docs scene get' (sent as the If-Match header for optimistic concurrency; required) |
| `--data` | json |  | elements: full Excalidraw elements to upsert; on a key collision the element with the higher `version` (then smaller `versionNonce`) wins the CAS. Each element MUST carry a valid fractional-index `index` (z-order key produced by the fractional-indexing / Excalidraw rules, e.g. `a0`); an index-less or malformed-index element is rejected locally before sending. Pass via --data. |

---

### `docs scene export`

Export a whiteboard's live scene as a PNG or SVG image (needs reader) · 🟢 read

```bash
octo-cli docs scene export <docId> [flags]
```

Renders the LIVE Excalidraw scene of a doc_type 'board' to an image on the server: `--image-format png` (default) returns a PNG, `--image-format svg` returns an SVG. The body is binary, so pass --output/-o to save it to a file (e.g. `docs scene export &lt;docId&gt; --image-format png -o board.png`); without --output the command only reports the response status, content-type and size. A non-board target returns 409 unsupported_doc_type; an unrecognised format returns 400 invalid_format.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--image-format` | enum(`png`, `svg`) |  | image format (one of: png, svg); default png. Exposed as --image-format so it does not shadow the global --format output flag. |

---

### `docs scene get`

Read a whiteboard's live scene + its base version (needs reader) · 🟢 read

```bash
octo-cli docs scene get <docId>
```

Reads the LIVE Excalidraw scene of a doc_type 'board' — its `elements` in fractional-index (z-order), the `files` map of image/file refs, and the base-version token that pins the read. Pass that token to `docs scene edit` as --base-version so the optimistic-concurrency guard can prove the scene did not change between read and write. doc / sheet bodies are a different Y.Doc shape and return 409 unsupported_doc_type here; a live scene that decodes to a wrong-kind / corrupt blob returns 409 board_snapshot_invalid.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

---

### `docs share get`

Read a document's space-level share scope (needs reader) · 🟢 read

```bash
octo-cli docs share get <docId>
```

Returns the current share settings any viewer can see: &#123;docId, shareScope, shareRole&#125;. shareScope is 'restricted' (only owner/members) or 'anyone_in_space' (every member of the doc's space gets the share role). shareRole is meaningful only when shareScope is anyone_in_space; a restricted doc always reports shareRole 'read'.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

---

### `docs share set`

Set a document's space-level share scope (needs admin) · 🟠 write

```bash
octo-cli docs share set <docId> [flags]
```

Change the share scope so either only owner/members can access (--scope restricted) or every member of the doc's space gets a share role (--scope anyone_in_space --role read\|edit). Normalization matches the backend: with --scope anyone_in_space a valid --role is required (a missing/invalid role is 400 invalid_role); with --scope restricted the role is ignored and stored as read, so --role may be omitted. An unknown scope is 400 invalid_scope; a non-admin caller is 403; a missing/cross-space doc is 404; an archived doc is 409. Share only ever RAISES access (owner/members are never downgraded).

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--scope` | enum(`restricted`, `anyone_in_space`) | ✓ | share scope to set |
| `--role` | enum(`read`, `edit`) |  | share role for anyone_in_space (required when scope=anyone_in_space; ignored and stored as read when scope=restricted) |

---

### `docs sheet edit`

Apply a batch sheet edit — cells / dims / drawings / hyperlinks (needs writer) · 🟠 write

```bash
octo-cli docs sheet edit <docId> [flags]
```

Applies a batch of cell, dimension, and/or drawing set/delete ops to a doc_type 'sheet' under a mandatory optimistic-concurrency guard. Pass the base-version token from `docs sheet get` via --base-version (sent as the If-Match header); the server rejects with 412 base_version_stale if the live sheet moved since that read. The cells batch is a JSON object `&#123; "sheetId!row:col": &#123;v,f,s&#125; \| null &#125;` (a null value deletes the cell). The optional dims batch sets column widths / row heights: a JSON object `&#123; "c&lt;idx&gt;"\|"r&lt;idx&gt;": &lt;px&gt; \| null &#125;` (e.g. `&#123;"c0":200,"r3":40&#125;`; a null value deletes that dim). The optional drawings batch inserts/removes floating images: a JSON object `&#123; "sheetId!drawingId": &lt;ISheetImage&gt; \| null &#125;` (image bytes ride inline as a base64 data URL in `source`; a math formula rides here too as a float-DOM drawing with `drawingType:8`, `componentKey:"octo-math-formula"`, and the LaTeX in `data.latex`). The optional hyperlinks batch sets cell links: a JSON object `&#123; "sheetId!linkId": &#123;id,row,column,payload,display?&#125; \| null &#125;` (payload is the URL — http/https/mailto or an internal #jump — and the link points at a cell by row/column; put the visible text in that cell's `v`). Provide `cells`, `dims`, `drawings`, or `hyperlinks` via --data; at least one must be non-empty. Only doc_type 'sheet' is accepted (doc / board / whiteboard return 409 unsupported_doc_type). Gates: cell/dim/drawing count over the max → 413 too_many_cells, a single cell's payload too large → 413 cell_too_large, missing base version / malformed shape / all batches empty → 400 invalid_body, a cell/dim/drawing violating its contract or key shape → 422 sheet_cell_invalid.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--base-version` | string | ✓ | base-version token from 'docs sheet get' (sent as the If-Match header for optimistic concurrency; required) |
| `--data` | json |  | cells: batch of cell ops keyed by cell key `sheetId!row:col` (e.g. `default!0:0`). Each value is either &#123;"v":&lt;string\|number\|boolean\|null&gt;,"f":"&lt;formula&gt;"?,"s":&lt;style object&gt;?&#125; to set the cell, or null to delete it. At least one of v/f/s must be present on a set. Pass via --data. |

---

### `docs sheet get`

Read a spreadsheet's live cells + dims + base version (needs reader) · 🟢 read

```bash
octo-cli docs sheet get <docId> [flags]
```

Reads the LIVE flat cell map of a doc_type 'sheet' (keys `sheetId!row:col`, values &#123;v,f,s&#125;), its column-width / row-height overrides (sheetDims), its cell hyperlinks (sheetHyperLinks), and the base-version token that pins the read. Pass that token to `docs sheet edit` as --base-version so the optimistic-concurrency guard can prove the sheet did not change between read and write. doc / board / whiteboard bodies are a different Y.Doc shape and return 409 unsupported_doc_type here. A whole-sheet read of a grid over the 1MB cap returns 413 sheet_too_large; pass --limit (and follow nextCursor via --cursor) to read it in pages instead. In paged mode each page is bounded by both --limit and the byte cap, sheetDims and sheetHyperLinks are returned on the first page only, and the response carries hasMore + nextCursor. A --cursor whose snapshot moved returns 409 sheet_changed (restart from the first page); a bad --cursor returns 400 invalid_cursor; a non-positive --limit returns 400 invalid_limit.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--limit` | integer |  | max cells per page; opts into paged reads. Bounded by the server byte cap regardless. Omit for a whole-sheet read. |
| `--cursor` | string |  | opaque nextCursor from the previous page; also opts into paged reads. A cursor whose snapshot moved returns 409 sheet_changed. |

---

### `docs versions create`

Take a named snapshot of the live document (needs writer) · 🟠 write

```bash
octo-cli docs versions create <docId> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--label` | string |  | human label for the snapshot (optional) |

---

### `docs versions delete`

Delete a saved version (needs admin) · 🟠 write

```bash
octo-cli docs versions delete <docId> <versionId>
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<versionId>` | path parameter |

---

### `docs versions list`

List saved versions/snapshots (needs reader) · 🟢 read

```bash
octo-cli docs versions list <docId> [flags]
```

Returns &#123;items, nextCursor, counts&#125;; there is no &#123;data, pagination&#125; envelope, so --page-all does not apply — pass --cursor with the returned nextCursor to walk pages.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--cursor` | integer |  | id cursor from a previous page's nextCursor |
| `--limit` | integer |  | page size |
| `--kind` | enum(`manual`, `auto`, `all`) |  | filter by snapshot kind |
| `--includeAuto` | string |  | back-compat alias honoured only when kind is absent; set to 1 to include auto snapshots |

---

### `docs versions rename`

Rename a saved version (needs writer) · 🟠 write

```bash
octo-cli docs versions rename <docId> <versionId> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<versionId>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--label` | string | ✓ | new non-empty label |

---

### `docs versions restore`

Restore the document to a saved version (needs admin) · 🟠 write

```bash
octo-cli docs versions restore <docId> <versionId>
```

Non-destructive forward reconcile of the target version into the live state. A safety snapshot of the pre-restore state is recorded first, so a restore is itself undoable.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<versionId>` | path parameter |

---

### `docs versions state`

Read a version's decoded content snapshot (read-only preview; needs reader) · 🟢 read

```bash
octo-cli docs versions state <docId> <versionId>
```

Returns the version decoded to structured content, kind-selected by the doc's doc_type. A document/sheet decodes to ProseMirror JSON (&#123;kind:'document', doc, sheetCells, sheetDims, schemaVersion, docVersionSeq&#125;); a board decodes to an Excalidraw scene (&#123;kind:'board', scene, schemaVersion, docVersionSeq&#125;). This is a read-only preview of a past snapshot, not the live document, and cannot be written.

**Arguments**

| Arg | Description |
|---|---|
| `<docId>` | path parameter |
| `<versionId>` | path parameter |

