---
title: "octo-cli matter"
description: "octo-cli commands in the matter domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/matter.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 14 commands, Generated from [`octo-cli/internal/registry/specs/matter.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/matter.json). Every invocation emits a structured JSON envelope on stdout.

### `matter assignee add`

Add an assignee to a matter · 🟠 write

```bash
octo-cli matter assignee add <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--user_id` | string | ✓ |  |

---

### `matter assignee remove`

Remove an assignee · 🟠 write

```bash
octo-cli matter assignee remove <id> <uid>
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |
| `<uid>` | path parameter |

---

### `matter channel link`

Link a channel to a matter · 🟠 write

```bash
octo-cli matter channel link <id> [flags]
```

Bot path is forbidden — only user tokens may link channels post-creation.

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | enum(`1`, `2`, `5`) | ✓ |  |
| `--channel_name` | string |  |  |

---

### `matter channel unlink`

Unlink a channel from a matter · 🟠 write

```bash
octo-cli matter channel unlink <id> <channel_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |
| `<channel_id>` | path parameter |

---

### `matter create`

Create a matter · 🟠 write

```bash
octo-cli matter create [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--title` | string | ✓ |  |
| `--description` | string |  |  |
| `--data` | json |  | assignee_ids: JSON payload |
| `--deadline` | string |  | RFC3339 timestamp |
| `--remind_at` | string |  | RFC3339 timestamp |
| `--source_channel_id` | string |  |  |
| `--source_channel_type` | enum(`1`, `2`, `5`) |  |  |
| `--source_name` | string |  |  |

---

### `matter delete`

Soft-delete a matter · 🔴 high-risk

```bash
octo-cli matter delete <id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

---

### `matter extract`

Extract matter(s) from chat messages via LLM · 🟠 write

```bash
octo-cli matter extract [flags]
```

Bot path requires creator_uid == bot's owner_uid. Rate-limited.

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | enum(`1`, `2`, `5`) | ✓ |  |
| `--channel_name` | string |  |  |
| `--creator_uid` | string | ✓ | For bot calls must equal bot owner UID |
| `--participant_uid` | string |  |  |
| `--data` | json | ✓ | msgs: JSON payload |

---

### `matter get`

Get matter detail · 🟢 read

```bash
octo-cli matter get <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--source_channel_id` | string |  | optional channel membership hint |

---

### `matter list`

List matters (cursor-paginated) · 🟢 read

```bash
octo-cli matter list [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--status` | enum(`open`, `done`, `archived`) |  | filter by matter status |
| `--assignee_id` | string |  | assignee UID (accepts 'me' to resolve to caller) |
| `--creator_id` | string |  | creator UID (accepts 'me') |
| `--q` | string |  | full-text search |
| `--source_channel_id` | string |  | filter by origin channel id |
| `--source_channel_type` | enum(`1`, `2`, `5`) |  | filter by origin channel type |
| `--channel_id` | string |  | filter by linked channel id |
| `--limit` | integer |  |  |
| `--cursor` | string |  | opaque pagination cursor |

---

### `matter timeline add`

Add a timeline entry (may include attachments or LLM extraction) · 🟠 write

```bash
octo-cli matter timeline add <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--content` | string |  |  |
| `--data` | json |  | attachments: JSON payload |
| `--channel_type` | enum(`1`, `2`, `5`) |  |  |
| `--channel_id` | string |  |  |
| `--channel_name` | string |  |  |
| `--participant_uid` | string |  |  |
| `--matter_id` | string |  |  |

---

### `matter timeline delete`

Delete a timeline entry · 🟠 write

```bash
octo-cli matter timeline delete <id> <entry_id>
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |
| `<entry_id>` | path parameter |

---

### `matter timeline list`

List timeline entries (cursor-paginated) · 🟢 read

```bash
octo-cli matter timeline list <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--source_channel_id` | string |  |  |
| `--limit` | integer |  |  |
| `--cursor` | string |  |  |

---

### `matter transition`

Transition matter status (open\|done\|archived) · 🟠 write

```bash
octo-cli matter transition <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--status` | enum(`open`, `done`, `archived`) | ✓ |  |

---

### `matter update`

Update matter fields · 🟠 write

```bash
octo-cli matter update <id> [flags]
```

**Arguments**

| Arg | Description |
|---|---|
| `<id>` | path parameter |

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--title` | string |  |  |
| `--description` | string |  |  |
| `--deadline` | string |  |  |
| `--remind_at` | string |  |  |

