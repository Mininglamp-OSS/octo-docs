---
title: "octo-cli bot"
description: "octo-cli commands in the bot domain — generated from the embedded OpenAPI spec."
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/bot.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 6 commands, generated from [`octo-cli/internal/registry/specs/bot.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/bot.json). Every invocation emits a structured JSON envelope on stdout.

Authenticate by exporting `OCTO_BOT_TOKEN` (`bf_…`) and `OCTO_API_BASE_URL`. See the [CLI overview](/reference/octo-cli) for common flags and the output contract.

### `bot heartbeat`

Report liveness (60s TTL in Redis) · 🟠 write

```bash
octo-cli bot heartbeat
```

---

### `bot register`

Register (authenticate) the bot and obtain IM token + URLs · 🟠 write

```bash
octo-cli bot register [flags]
```

Routes by token prefix: app_* → App Bot, bf_* → User Bot.

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--agent_platform` | string |  |  |
| `--agent_version` | string |  |  |
| `--plugin_version` | string |  |  |

---

### `bot set-commands`

Publish the bot's command list · 🟠 write

```bash
octo-cli bot set-commands [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--data` | json | ✓ | commands: JSON payload |

---

### `bot space-members`

List members of the bot's space · 🟢 read

```bash
octo-cli bot space-members [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--keyword` | string |  | optional LIKE filter on name |
| `--space_id` | string |  | override space (defaults to bot's space) |
| `--limit` | integer |  |  |

---

### `bot typing`

Send a typing indicator (throttled server-side) · 🟠 write

```bash
octo-cli bot typing [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | integer | ✓ |  |
| `--on_behalf_of` | string |  | OBO: send the typing indicator as this user persona |

---

### `bot user-info`

Look up a user by UID · 🟢 read

```bash
octo-cli bot user-info [flags]
```

**Flags**

| Flag | Type | Required | Description |
|---|---|---|---|
| `--uid` | string | ✓ |  |

