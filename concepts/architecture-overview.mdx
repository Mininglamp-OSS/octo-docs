---
title: "Architecture overview"
description: "The Octo service map and the full request lifecycle, from client to IM fan-out."
icon: "sitemap"
---

Octo is a set of focused services that meet at one anchor: **`octo-server`**. It exposes REST +
WebSocket APIs to the clients, orchestrates business logic and Lobster (AI agent) scheduling,
and drives [WuKongIM](/concepts/messaging-and-im-core) for real-time messaging.

## Service map

Everything meets at **`octo-server`**. Clients enter through nginx; the server orchestrates the
core services, drives the messaging core, owns the data plane, and admits agents and integrations
at well-defined seams.

```mermaid
graph LR
  subgraph clients["Clients"]
    web["octo-web · Web / PC"]
    mob["octo-android · octo-ios"]
    ext["octo-chrome-extension"]
    adm["octo-admin"]
  end

  nginx["nginx · reverse proxy :28080"]

  subgraph core["Application core"]
    server["octo-server<br/>REST + WS · orchestration · Lobster scheduling"]
    summary["octo-smart-summary"]
    speech["octo-speech"]
    market["octo-marketplace"]
    dbe["octo-docs-backend · Yjs / Hocuspocus"]
    dhtml["octo-docs-html"]
  end

  im["WuKongIM · octo-im<br/>messaging core"]

  subgraph agents["Agent plane"]
    chan["Channels · cc / codex / hermes / claw / openclaw"]
    cli["octo-cli"]
    daemon["octo-daemon-cli"]
  end

  subgraph pipe["Search & export · optional"]
    prod["searchetl-producer"]
    esi["es-indexer"]
    exp["octo-message-export-api"]
  end

  subgraph data["Data plane"]
    my[("MySQL")]
    rd[("Redis")]
    obj[("MinIO / S3")]
    kf[("Kafka")]
    osx[("OpenSearch")]
  end

  subgraph shared["Shared libraries & integration"]
    lib["octo-lib"]
    auth["octo-auth"]
    adp["octo-adapters"]
  end

  web --> nginx
  mob --> nginx
  ext --> nginx
  adm --> nginx
  nginx --> server
  nginx --> market
  nginx -->|/ws| im
  server --> im
  server --> summary
  server --> speech
  server --> dbe
  market -. auth .-> server
  web -. collab ws .-> dbe
  server --> my
  server --> rd
  server --> obj
  chan --> server
  cli --> server
  daemon --> server
  prod -. reads .-> my
  prod -->|octo.message.v1| kf
  kf --> esi
  esi --> osx
  osx -. read .-> server
  osx -. read .-> exp
  exp --> obj
  server -. uses .-> lib
  server -. uses .-> auth
  server -. uses .-> adp
```

Clients and sibling services **all speak to `octo-server`**; deploy and scale one backend and
everything else talks to it. Storage and IM are pluggable: MySQL-compatible migrations and
object-storage adapters ship in the box, and WuKongIM is driven over a thin control-plane
boundary so the IM core stays swappable. The **search & export** pipeline is optional —
`octo-search-indexer`'s two binaries (`searchetl-producer` and `es-indexer`) move messages from
the store through Kafka into OpenSearch, which `octo-server` queries and `octo-message-export-api`
serves as batch exports to object storage.

<Note>
  This is the **runtime** view. Dev-time standards ([octo-spec](/reference/octo-spec-okf),
  [octo-skills](/guides/bot-developers/publish-a-skill)), packaging
  ([octo-deployment](/guides/operators/deploy-compose)), and the marketing site are part of the
  ecosystem but not runtime components — see the [repository guide](/ecosystem/repository-guide)
  for the full catalog.
</Note>

## The request lifecycle

Every request through `octo-server` follows the same path:

<Steps>
  <Step title="Authenticate">
    Auth middleware resolves the credential — a Redis-cached token parser also resolves the
    caller's language and role. See [Security & auth](/concepts/security-and-auth).
  </Step>
  <Step title="Authorise">
    Space middleware enforces org-aware RBAC (member / admin / owner), per-channel ACL, and
    agent-identity gating. Handlers that touch user data must pass through it.
  </Step>
  <Step title="Execute">
    Business logic runs — possibly spawning or resuming a Lobster (bot) session. See
    [The Lobster model](/concepts/the-lobster-model).
  </Step>
  <Step title="Fan out">
    The message is enqueued to WuKongIM (over gRPC); if a channel requires an external bridge,
    the relevant [adapter / channel](/guides/bot-developers/choose-a-channel) is triggered.
  </Step>
  <Step title="Respond">
    A unified JSON envelope (or WebSocket frame) is returned, with tracing and metrics tags,
    and rate-limited across three layers (per-IP, per-endpoint, per-UID).
  </Step>
</Steps>

## How the server is built

`octo-server` is organized as **27+ auto-registered modules** under `modules/` (each wires
itself in via `init()` + `register.AddModule()`) rather than one monolith. Bot identity,
on-behalf-of orchestration, spaces, threads, and BotFather each live in their own module.

<Card title="Deploy the whole thing" icon="rocket" href="/get-started/quickstart-deploy">
  See every service running together in one Compose stack.
</Card>
