---
title: "配置参考"
description: "octo-server 的每一个配置项、默认值与说明 —— 由配置模板生成。"
icon: "sliders"
---

{/*
  DO NOT EDIT — generated by scripts/gen-config.mjs from the source of truth:
  octo-server/configs/tsdd.yaml
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

由带注释的配置模板 [`octo-server/configs/tsdd.yaml`](https://github.com/Mininglamp-OSS/octo-server/blob/main/configs/tsdd.yaml) 生成 —— **14 个分组、共 133 个配置项**。

<Info>
  配置项以点分形式展示(例如 `db.mysqlAddr`)。模板中大多数项默认被注释,回退到内置默认值;标注 _(active default)_ 的项在随附模板中被显式设置。许多项还支持环境变量覆盖 —— 参见内联说明与[部署指南](/zh/guides/operators/deploy-compose)。
</Info>

## General

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `mode` | `"debug"` | 运行模式 debug or release _(默认已启用)_ |
| `adminpwd` | `"123456"` | 管理员密码 |
| `addr` | `":8090"` | api监听地址 |
| `grpcAddr` | `"0.0.0.0:6979"` | webhook grpc监听地址 给悟空IM提供的 |
| `appName` | `"DMWork"` | 项目名称 |
| `rootDir` | `"tsdddata"` | 数据根目录 |
| `messageSaveAcrossDevice` | `true` | 消息是否跨设备保存（换设备登录消息是否还能同步到老消息） |
| `welcomeMessage` | `"欢迎使用{{appName}}"` | 欢迎消息 |
| `phoneSearchOff` | `true` | 关闭手机号搜索用户功能 |
| `onlineStatusOn` | `true` | 开启在线状态功能 |
| `groupUpgradeWhenMemberCount` | `1000` | 群组人数达到多少人时，群组自动升级为超级群组 |
| `eventPoolSize` | `100` | 事件池大小 |

## Webhook安全配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `webhookSecretKey` | `""` | 留空则不验证签名 |

## 悟空IM配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `wukongIM.apiURL` | `""` | 悟空IM的api地址 格式： `http://xx.xx.xx.xx:5001` |
| `wukongIM.managerToken` | `""` | 悟空IM的管理者token 悟空IM配置了就需要填写，没配置就不需要 |

## db

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `db.mysqlAddr` | `"root:demo@tcp(127.0.0.1:3306)/test?charset=utf8mb4&parseTime=true"` | mysql连接地址 |
| `db.redisAddr` | `""` | redis地址 |
| `db.redisPass` | `""` | redis密码 |
| `db.redisTLS` | `false` | 启用 TLS（rediss://），用于 AWS ElastiCache / Azure Cache 等托管服务 |
| `db.redisTLSInsecureSkipVerify` | `false` | 跳过 TLS 证书校验（仅用于测试 / 自签名证书） |
| `db.redisTLSCAFile` | `""` | 自定义 CA 证书路径（可选，留空走系统 CA 池） |
| `db.asynctaskRedisAddr` | `""` | 异步任务的redis地址 不写默认为RedisAddr的地址 |

## 外网配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `external.ip` | `""` | 外网ip；仅当 baseURL 空时作为兜底 host（见 pkg/botutil/connect.go） |
| `external.baseURL` | `""` | 外网 API 访问地址 例如 `http://192.0.2.1:8090（baseURL` 已给出时 ip 冗余，两者保留是历史兜底） |
| `external.webLoginURL` | `""` | web im 的登录/门户地址 例如 `https://web.tangsengdaodao.com` |

## 日志配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `logger.level` | `0` | 日志级别 0:未配置,将根据mode属性判断 1:debug 2:info 3:warn 4:error |
| `logger.dir` | `"./logs"` | 日志目录 |
| `logger.lineNum` | `false` | 是否打印行号 |

## 短信配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `smsCode` | `"123456"` | 测试短信验证码， 如果不为空，则短信验证码为该值。 _(默认已启用)_ |
| `smsProvider` | `"aliyun"` | 短信服务商. 例如: aliyun or unisms |
| `aliyunSMS.accessKeyID` | `""` | 阿里云短信accessKeyID |
| `aliyunSMS.accessSecret` | `""` | 阿里云短信accessSecret |
| `aliyunSMS.templateCode` | `""` | 阿里云短信模板code 验证码变量名为code |
| `aliyunSMS.signName` | `""` | 阿里云短信签名 |
| `aliyunInternationalSMS` | — | 阿里云国际短信，如果配置了该项，遇到国际区号的手机号，将使用该配置发送短信 |
| `aliyunInternationalSMS.accessKeyID` | `""` | 阿里云国际短信accessKeyID |
| `aliyunInternationalSMS.accessSecret` | `""` | 阿里云国际短信accessSecret |
| `uniSMS.accessKeyID` | `""` | unisms accessKeyID |
| `uniSMS.signature` | `""` | unisms signature |
| `uniSMS.accessKeySecret` | `""` | unisms accessKeySecret 简易模式可以为空 |
| `uniSMS.templateId` | `""` | unisms TemplateId 验证码变量名为code |

## 文件服务

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `fileService` | `"minio"` | 文件服务 minio or aliyunOSS or seaweedFS or qiniu |
| `minio` | — | minio配置 |
| `minio.url` | `""` | minio地址 格式：`http://xx.xx.xx.xx:9000` (server-internal endpoint) |
| `minio.uploadURL` | `""` | 可选：服务端上传基地址，留空则回退到 url。仅服务端使用。 |
| `minio.downloadURL` | `""` | 浏览器可访问的 MinIO 基地址，用于签发预签名 PUT/GET URL。 |
| `minio.accessKeyID` | `""` | minio accessKeyID |
| `minio.secretAccessKey` | `""` | minio secretAccessKey |
| `oss` | — | aliyun oss配置 |
| `oss.endpoint` | `""` | oss endpoint 例如 oss-cn-hangzhou.aliyuncs.com |
| `oss.bucketURL` | `""` | oss bucketURL 例如 `https://xxxx.oss-cn-hangzhou.aliyuncs.com` |
| `oss.accessKeyID` | `""` | oss accessKeyID |
| `oss.accessKeySecret` | `""` | oss accessKeySecret |
| `seaweed` | — | seaweed配置 |
| `seaweed.url` | `""` | seaweed地址 格式：`http://xx.xx.xx.xx:9000` |
| `qiniu` | — | 七牛云配置 |
| `qiniu.url` | `""` | 七牛云对象存储请求域名，例：`http://cdn.tsdaodao.com` |
| `qiniu.bucketName` | `""` | 七牛云对象存储 bucket空间名称 |
| `qiniu.accessKey` | `""` | 七牛云密钥 AccessKey |
| `qiniu.secretKey` | `""` | 七牛云密钥 secretKey |

## 推送配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `push.contentDetailOn` | `true` | 推送内容是否显示详情 |
| `push.pushPoolSize` | `100` | 推送池大小 |
| `push.apns` | — | 苹果推送 |
| `push.apns.dev` | `false` | 是否为开发环境 |
| `push.apns.topic` | `""` | topic 例如： com.xinbida.tangsengdaodao |
| `push.apns.password` | `""` |  |
| `push.apns.cert` | `""` | apns证书路径 例如：configs/push/push.p12 |
| `push.hms` | — | 华为推送 |
| `push.hms.packageName` | `""` | 华为推送包名 例如：com.xinbida.tangsengdaodao |
| `push.hms.appID` | `""` | 华为推送appID |
| `push.hms.appSecret` | `""` | 华为推送appSecret |
| `push.mi` | — | 小米推送 |
| `push.mi.packageName` | `""` | 小米推送包名 例如：com.xinbida.tangsengdaodao |
| `push.mi.appID` | `""` | 小米推送appID |
| `push.mi.appSecret` | `""` | 小米推送appSecret |
| `push.mi.channelID` | `""` | 小米推送channelID |
| `push.vivo` | — | vivo推送 |
| `push.vivo.packageName` | `""` | vivo推送包名 例如：com.xinbida.tangsengdaodao |
| `push.vivo.appID` | `""` | vivo推送appID |
| `push.vivo.appKey` | `""` | vivo推送appKey |
| `push.vivo.appSecret` | `""` | vivo推送appSecret |
| `push.oppo` | — | oppo推送 |
| `push.oppo.packageName` | `""` | oppo推送包名 例如：com.xinbida.tangsengdaodao |
| `push.oppo.appID` | `""` | oppo推送appID |
| `push.oppo.appKey` | `""` | oppo推送appKey |
| `push.oppo.appSecret` | `""` | oppo推送appSecret |
| `push.oppo.masterSecret` | `""` | oppo推送masterSecret |
| `push.firebase` | — | 小米推送 |
| `push.firebase.packageName` | `""` | 小米推送包名 例如：com.xinbida.tangsengdaodao |
| `push.firebase.jsonPath` | `""` | serviceAccount的JSON文件路径 例如：configs/push/fcm_test.json |
| `push.firebase.projectId` | `""` | serviceAccount的JSON中的project_id值 |
| `push.firebase.channelID` | `""` | 忽略占位 |

## 注册

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `register.off` | `false` | 是否关闭注册 |
| `register.onlyChina` | `false` | 是否只允许中国手机号注册 |
| `register.stickerAddOff` | `false` | 是否关闭注册添加表情 |

## 内置账户配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `account.systemUID` | `"u_10000"` | 系统账户uid |
| `account.fileHelperUID` | `"fileHelper"` | 文件助手uid |
| `account.systemGroupID` | `"g_10000"` | 系统群组id |
| `account.systemGroupName` | `"意见反馈群"` | 系统群组名称 |
| `account.adminUID` | `"admin"` | 管理员uid |

## 头像

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `avatar.defaultBaseURL` | `""` | 默认头像cdn地址 |
| `avatar.default` | `"assets/assets/avatar.png"` | 默认头像 |
| `avatar.defaultCount` | `900` | 默认头像数量 |
| `avatar.partition` | `100` | 头像分区数量 |

## 短号配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `shortNo.numOn` | `false` | 短号是否为纯数字 |
| `shortNo.numLen` | `7` | 短号长度 |
| `shortNo.editOff` | `false` | 是否允许修改短号 |

## 机器人配置

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `robot.eventPoolSize` | `100` | 事件池大小 |
| `gitee.oauthURL` | `"https://gitee.com/oauth/authorize"` | gitee oauth地址 |
| `gitee.clientID` | `""` | gitee client id |
| `gitee.clientSecret` | `""` | gitee client secret |
| `github.oauthURL` | `"https://github.com/login/oauth/authorize"` | github oauth url |
| `github.clientID` | `""` | github client id |
| `github.clientSecret` | `""` | github client secret |
| `cache.tokenCachePrefix` | `"token:"` | token缓存前缀 |
| `cache.loginDeviceCachePrefix` | `"login_device:"` | 登录设备缓存前缀 |
| `cache.uidTokenCachePrefix` | `"uidtoken:"` | uid和token的缓存前缀 |
| `cache.friendApplyTokenCachePrefix` | `"friend_token:"` | 好友申请token缓存前缀 |
