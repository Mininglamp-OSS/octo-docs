---
title: "octo-cli 命令参考"
description: "跨所有领域的全部 octo-cli 命令 —— 由内嵌的 OpenAPI 规范生成。"
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/*.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

[`octo-cli`](https://github.com/Mininglamp-OSS/octo-cli) 是一个单二进制 REST 客户端,专为 AI Agent 机器人通过 `exec` 调用而设计。它的命令树 —— **8 个领域、共 84 条命令** —— 在启动时由内嵌的 OpenAPI 规范自动注册,因此本参考始终与 CLI 和服务端保持同步。

<Info>
  每次调用都会在 stdout 输出结构化的 JSON 信封;错误输出到 stderr,并带有确定性的分类。没有交互式 I/O —— 非常适合 Agent 运行时。可与 [REST API 参考](/zh/reference/rest-websocket-api) 搭配使用(每条命令对应一个 API 接口)。
</Info>

## 通用标志

| 标志 | 用途 |
|---|---|
| `--data <json>` | 用于创建/更新命令的 JSON 请求体。 |
| `--jq <expr>` | 用 jq 表达式对 JSON 信封做后处理。 |

## 领域

| 命令组 | 命令数 |
|---|---|
| [`octo-cli message`](/zh/reference/cli/message) | 4 |
| [`octo-cli thread`](/zh/reference/cli/thread) | 8 |
| [`octo-cli group`](/zh/reference/cli/group) | 9 |
| [`octo-cli file`](/zh/reference/cli/file) | 4 |
| [`octo-cli event`](/zh/reference/cli/event) | 2 |
| [`octo-cli bot`](/zh/reference/cli/bot) | 6 |
| [`octo-cli docs`](/zh/reference/cli/docs) | 31 |
| [`octo-cli html`](/zh/reference/cli/html) | 20 |
| **合计** | **84** |
