---
title: "事件 API"
description: "事件 领域的接口 —— 由 octo-cli OpenAPI 规范生成。"
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/event.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 由以下规范生成： [`octo-cli/internal/registry/specs/event.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/event.json) — **2 个接口**.

### Poll pending events (server-side cursor by event_id)

`POST /v1/bot/events` · CLI 命令: `event.list` · 🟢 读

Uses POST despite being a read — event_id cursor is carried in the request body.

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `event_id` | integer |  | exclusive lower bound; 0 for initial poll |
| `limit` | integer |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Event batch |

---

### Acknowledge (remove) an event from the queue

`POST /v1/bot/events/{event_id}/ack` · CLI 命令: `event.ack` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `event_id` | path | integer | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Acknowledged |

