---
title: "事项 API"
description: "事项 领域的接口 —— 由 octo-cli OpenAPI 规范生成。"
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/matter.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 由以下规范生成： [`octo-cli/internal/registry/specs/matter.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/matter.json) — **14 个接口**.

### List matters (cursor-paginated)

`GET /api/v1/matters` · CLI 命令: `matter.list` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `status` | query | enum(`open`, `done`, `archived`) |  | filter by matter status |
| `assignee_id` | query | string |  | assignee UID (accepts 'me' to resolve to caller) |
| `creator_id` | query | string |  | creator UID (accepts 'me') |
| `q` | query | string |  | full-text search |
| `source_channel_id` | query | string |  | filter by origin channel id |
| `source_channel_type` | query | enum(`1`, `2`, `5`) |  | filter by origin channel type |
| `channel_id` | query | string |  | filter by linked channel id |
| `limit` | query | integer |  |  |
| `cursor` | query | string |  | opaque pagination cursor |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Paginated matter list |

---

### Create a matter

`POST /api/v1/matters` · CLI 命令: `matter.create` · 🟠 写

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `title` | string | ✓ |  |
| `description` | string |  |  |
| `assignee_ids` | string[] |  |  |
| `deadline` | string |  | RFC3339 timestamp |
| `remind_at` | string |  | RFC3339 timestamp |
| `source_channel_id` | string |  |  |
| `source_channel_type` | enum(`1`, `2`, `5`) |  |  |
| `source_name` | string |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `201` | Matter created |

---

### Extract matter(s) from chat messages via LLM

`POST /api/v1/matters/extract` · CLI 命令: `matter.extract` · 🟠 写

Bot path requires creator_uid == bot's owner_uid. Rate-limited.

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `channel_id` | string | ✓ |  |
| `channel_type` | enum(`1`, `2`, `5`) | ✓ |  |
| `channel_name` | string |  |  |
| `creator_uid` | string | ✓ | For bot calls must equal bot owner UID |
| `participant_uid` | string |  |  |
| `msgs` | object[] | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `201` | Extraction result |

---

### Get matter detail

`GET /api/v1/matters/{id}` · CLI 命令: `matter.get` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |
| `source_channel_id` | query | string |  | optional channel membership hint |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Matter detail |

---

### Update matter fields

`PUT /api/v1/matters/{id}` · CLI 命令: `matter.update` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `title` | string |  |  |
| `description` | string |  |  |
| `deadline` | string |  |  |
| `remind_at` | string |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Updated matter |

---

### Soft-delete a matter

`DELETE /api/v1/matters/{id}` · CLI 命令: `matter.delete` · 🔴 高危

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Deleted |

---

### Transition matter status (open\|done\|archived)

`PUT /api/v1/matters/{id}/status` · CLI 命令: `matter.transition` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `status` | enum(`open`, `done`, `archived`) | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Matter detail after transition |

---

### Add an assignee to a matter

`POST /api/v1/matters/{id}/assignees` · CLI 命令: `matter.assignee.add` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `user_id` | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Added |

---

### Remove an assignee

`DELETE /api/v1/matters/{id}/assignees/{uid}` · CLI 命令: `matter.assignee.remove` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |
| `uid` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Removed |

---

### Link a channel to a matter

`POST /api/v1/matters/{id}/channels` · CLI 命令: `matter.channel.link` · 🟠 写

Bot path is forbidden — only user tokens may link channels post-creation.

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `channel_id` | string | ✓ |  |
| `channel_type` | enum(`1`, `2`, `5`) | ✓ |  |
| `channel_name` | string |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `201` | Linked |

---

### Unlink a channel from a matter

`DELETE /api/v1/matters/{id}/channels/{channel_id}` · CLI 命令: `matter.channel.unlink` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |
| `channel_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Unlinked |

---

### List timeline entries (cursor-paginated)

`GET /api/v1/matters/{id}/timeline` · CLI 命令: `matter.timeline.list` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |
| `source_channel_id` | query | string |  |  |
| `limit` | query | integer |  |  |
| `cursor` | query | string |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Paginated timeline |

---

### Add a timeline entry (may include attachments or LLM extraction)

`POST /api/v1/matters/{id}/timeline` · CLI 命令: `matter.timeline.add` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `content` | string |  |  |
| `attachments` | —[] |  |  |
| `channel_type` | enum(`1`, `2`, `5`) |  |  |
| `channel_id` | string |  |  |
| `channel_name` | string |  |  |
| `participant_uid` | string |  |  |
| `matter_id` | string |  |  |
| `msgs` | object[] |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `201` | Created entry (or LLM result) |

---

### Delete a timeline entry

`DELETE /api/v1/matters/{id}/timeline/{entry_id}` · CLI 命令: `matter.timeline.delete` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | path | string | ✓ |  |
| `entry_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Deleted |

