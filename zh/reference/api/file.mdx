---
title: "文件 API"
description: "文件 领域的接口 —— 由 octo-cli OpenAPI 规范生成。"
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/file.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 由以下规范生成： [`octo-cli/internal/registry/specs/file.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/file.json) — **4 个接口**.

用机器人 token(`bf_…`)作为 bearer 凭证调用这些接口，或通过 [`octo-cli`](/zh/guides/bot-developers/drive-octo-with-cli) 调用。鉴权与风险级别见 [API 总览](/zh/reference/rest-websocket-api)。

### Upload a file via multipart form

`POST /v1/bot/file/upload` · CLI 命令: `file.upload` · 🟠 写

multipart/form-data: file=&lt;binary&gt;. Max 100MB.

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `type` | query | string |  | storage bucket subdir |
| `path` | query | string |  | override generated path |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Uploaded |

---

### Download a file (302 redirect to presigned URL)

`GET /v1/bot/file/download/{path}` · CLI 命令: `file.download` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `path` | path | string | ✓ | storage key path (URL-encoded) |
| `filename` | query | string |  | override download filename |

**响应**

| 状态 | 说明 |
|---|---|
| `302` | Redirect to presigned URL |

---

### Obtain STS temp credentials for direct-to-COS upload

`GET /v1/bot/upload/credentials` · CLI 命令: `file.credentials` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `filename` | query | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | STS credentials |

---

### Get a presigned PUT URL for direct upload

`GET /v1/bot/upload/presigned` · CLI 命令: `file.presigned` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `filename` | query | string | ✓ |  |
| `fileSize` | query | integer | ✓ | file size in bytes; required, must be a positive integer within the server max |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Presigned URL |

