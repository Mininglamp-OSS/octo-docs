---
title: "群组 API"
description: "群组 领域的接口 —— 由 octo-cli OpenAPI 规范生成。"
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/group.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 由以下规范生成： [`octo-cli/internal/registry/specs/group.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/group.json) — **9 个接口**.

用机器人 token(`bf_…`)作为 bearer 凭证调用这些接口，或通过 [`octo-cli`](/zh/guides/bot-developers/drive-octo-with-cli) 调用。鉴权与风险级别见 [API 总览](/zh/reference/rest-websocket-api)。

### List groups the bot is a member of

`GET /v1/bot/groups` · CLI 命令: `group.list` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `space_id` | query | string |  | optional filter by space |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Group list |

---

### Get group info

`GET /v1/bot/groups/{group_no}` · CLI 命令: `group.get` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Group info |

---

### List group members

`GET /v1/bot/groups/{group_no}/members` · CLI 命令: `group.members` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Members |

---

### Read GROUP.md content

`GET /v1/bot/groups/{group_no}/md` · CLI 命令: `group.md-get` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | GROUP.md |

---

### Update GROUP.md content (bot must be bot_admin)

`PUT /v1/bot/groups/{group_no}/md` · CLI 命令: `group.md-update` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `content` | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | New version |

---

### Create a new group (User Bot only; App Bot rejected)

`POST /v1/bot/createGroup` · CLI 命令: `group.create` · 🟠 写

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string |  |  |
| `members` | string[] | ✓ |  |
| `creator` | string |  |  |
| `space_id` | string |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Created group |

---

### Update group name/notice (bot must be bot_admin; App Bot rejected)

`PUT /v1/bot/groups/{group_no}/info` · CLI 命令: `group.update` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string |  |  |
| `notice` | string |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Updated |

---

### Add members to group (App Bot rejected)

`POST /v1/bot/groups/{group_no}/members/add` · CLI 命令: `group.member-add` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `members` | string[] | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Added |

---

### Remove members from group (bot must be bot_admin; App Bot rejected)

`POST /v1/bot/groups/{group_no}/members/remove` · CLI 命令: `group.member-remove` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `members` | string[] | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Removed |

