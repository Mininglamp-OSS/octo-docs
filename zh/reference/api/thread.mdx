---
title: "话题 API"
description: "话题 领域的接口 —— 由 octo-cli OpenAPI 规范生成。"
icon: "code"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/thread.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 由以下规范生成： [`octo-cli/internal/registry/specs/thread.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/thread.json) — **8 个接口**.

### List threads in a group (User Bot only)

`GET /v1/bot/groups/{group_no}/threads` · CLI 命令: `thread.list` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `page_index` | query | integer |  |  |
| `page_size` | query | integer |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Thread list |

---

### Create a thread in a group (User Bot only)

`POST /v1/bot/groups/{group_no}/threads` · CLI 命令: `thread.create` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✓ |  |
| `source_message_id` | integer |  |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Created thread |

---

### Get thread detail (User Bot only)

`GET /v1/bot/groups/{group_no}/threads/{short_id}` · CLI 命令: `thread.get` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `short_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Thread detail |

---

### List thread members (User Bot only)

`GET /v1/bot/groups/{group_no}/threads/{short_id}/members` · CLI 命令: `thread.members` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `short_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Members |

---

### Bot joins a thread (User Bot only)

`POST /v1/bot/groups/{group_no}/threads/{short_id}/join` · CLI 命令: `thread.join` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `short_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Joined |

---

### Bot leaves a thread (User Bot only)

`POST /v1/bot/groups/{group_no}/threads/{short_id}/leave` · CLI 命令: `thread.leave` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `short_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | Left |

---

### Read thread GROUP.md (User Bot only)

`GET /v1/bot/groups/{group_no}/threads/{short_id}/md` · CLI 命令: `thread.md-get` · 🟢 读

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `short_id` | path | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | GROUP.md |

---

### Update thread GROUP.md (User Bot only; must be bot_admin)

`PUT /v1/bot/groups/{group_no}/threads/{short_id}/md` · CLI 命令: `thread.md-update` · 🟠 写

**参数**

| 名称 | 位置 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `group_no` | path | string | ✓ |  |
| `short_id` | path | string | ✓ |  |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `content` | string | ✓ |  |

**响应**

| 状态 | 说明 |
|---|---|
| `200` | New version |

