---
title: "REST 与 WebSocket API"
description: "Octo 权威 REST API —— 由 octo-cli 内嵌的 OpenAPI 3.1 规范生成。"
icon: "plug"
---

{/*
  DO NOT EDIT — generated by scripts/gen-api.mjs from the source of truth:
  octo-cli/internal/registry/specs/*.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

Octo 面向机器人的 REST API 覆盖 **8 个领域、共 84 个接口**。侧边栏中的每个领域都是一份由内嵌于 [`octo-cli`](https://github.com/Mininglamp-OSS/octo-cli) 的 OpenAPI 3.1 规范生成的**交互式参考** —— 与驱动 CLI 和服务端的是同一份规范,因此永不与实现漂移。打开任一接口即可查看其 schema 并发起实时请求。

<Tip>
  用机器人 token(`bf_…`)作为 bearer 凭证调用这些接口 —— 在某个接口的 **Try it** 面板中填入你的实例地址与 token 即可发起实时请求,或使用 [`octo-cli`](/zh/guides/bot-developers/drive-octo-with-cli)。参见 [用 octo-auth 校验凭证](/zh/guides/integrators/verify-credentials-with-octo-auth)。这些操作读写的实体与枚举,汇总于[数据模型](/zh/reference/data-model)。
</Tip>

## 领域

| 领域 | 规范 | 接口数 |
|---|---|---|
| 消息 | `message` | 4 |
| 话题 | `thread` | 8 |
| 群组 | `group` | 9 |
| 文件 | `file` | 4 |
| 事件 | `event` | 2 |
| 机器人身份 | `bot` | 6 |
| 协作文档 | `docs` | 31 |
| 交互式 HTML 文档 | `html` | 20 |
| **合计** | | **84** |

## 风险级别

每个接口都带有来自规范的 `x-octo-risk` 级别标记:

- 🟢 **读** —— 不改变状态。
- 🟠 **写** —— 创建或修改状态。
- 🔴 **高危** —— 破坏性或作用域较广,请谨慎。

## 鉴权

凭证类型(会话 · `bf_` 机器人 token · `uk_` API key)与统一错误信封,参见 [错误与响应信封](/zh/reference/errors-and-envelopes)。
