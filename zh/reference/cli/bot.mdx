---
title: "octo-cli bot"
description: "bot 领域的 octo-cli 命令 —— 由内嵌的 OpenAPI 规范生成。"
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/bot.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 6 条命令, 由以下规范生成： [`octo-cli/internal/registry/specs/bot.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/bot.json). 每次调用都会在 stdout 输出结构化的 JSON 信封。

### `bot heartbeat`

Report liveness (60s TTL in Redis) · 🟠 写

```bash
octo-cli bot heartbeat
```

---

### `bot register`

Register (authenticate) the bot and obtain IM token + URLs · 🟠 写

```bash
octo-cli bot register [flags]
```

Routes by token prefix: app_* → App Bot, bf_* → User Bot.

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--agent_platform` | string |  |  |
| `--agent_version` | string |  |  |
| `--plugin_version` | string |  |  |

---

### `bot set-commands`

Publish the bot's command list · 🟠 写

```bash
octo-cli bot set-commands [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--data` | json | ✓ | commands: JSON payload |

---

### `bot space-members`

List members of the bot's space · 🟢 读

```bash
octo-cli bot space-members [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--keyword` | string |  | optional LIKE filter on name |
| `--space_id` | string |  | override space (defaults to bot's space) |
| `--limit` | integer |  |  |

---

### `bot typing`

Send a typing indicator (throttled server-side) · 🟠 写

```bash
octo-cli bot typing [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--channel_id` | string | ✓ |  |
| `--channel_type` | integer | ✓ |  |
| `--on_behalf_of` | string |  | OBO: send the typing indicator as this user persona |

---

### `bot user-info`

Look up a user by UID · 🟢 读

```bash
octo-cli bot user-info [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--uid` | string | ✓ |  |

