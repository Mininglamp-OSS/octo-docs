---
title: "octo-cli thread"
description: "thread 领域的 octo-cli 命令 —— 由内嵌的 OpenAPI 规范生成。"
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/thread.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 8 条命令, 由以下规范生成： [`octo-cli/internal/registry/specs/thread.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/thread.json). 每次调用都会在 stdout 输出结构化的 JSON 信封。

### `thread create`

Create a thread in a group (User Bot only) · 🟠 写

```bash
octo-cli thread create <group_no> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--name` | string | ✓ |  |
| `--source_message_id` | integer |  |  |

---

### `thread get`

Get thread detail (User Bot only) · 🟢 读

```bash
octo-cli thread get <group_no> <short_id>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |
| `<short_id>` | 路径参数 |

---

### `thread join`

Bot joins a thread (User Bot only) · 🟠 写

```bash
octo-cli thread join <group_no> <short_id>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |
| `<short_id>` | 路径参数 |

---

### `thread leave`

Bot leaves a thread (User Bot only) · 🟠 写

```bash
octo-cli thread leave <group_no> <short_id>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |
| `<short_id>` | 路径参数 |

---

### `thread list`

List threads in a group (User Bot only) · 🟢 读

```bash
octo-cli thread list <group_no> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--page_index` | integer |  |  |
| `--page_size` | integer |  |  |

---

### `thread md-get`

Read thread GROUP.md (User Bot only) · 🟢 读

```bash
octo-cli thread md-get <group_no> <short_id>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |
| `<short_id>` | 路径参数 |

---

### `thread md-update`

Update thread GROUP.md (User Bot only; must be bot_admin) · 🟠 写

```bash
octo-cli thread md-update <group_no> <short_id> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |
| `<short_id>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--content` | string | ✓ |  |

---

### `thread members`

List thread members (User Bot only) · 🟢 读

```bash
octo-cli thread members <group_no> <short_id>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |
| `<short_id>` | 路径参数 |

