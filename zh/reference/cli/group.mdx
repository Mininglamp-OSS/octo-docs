---
title: "octo-cli group"
description: "group 领域的 octo-cli 命令 —— 由内嵌的 OpenAPI 规范生成。"
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/group.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 9 条命令, 由以下规范生成： [`octo-cli/internal/registry/specs/group.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/group.json). 每次调用都会在 stdout 输出结构化的 JSON 信封。

鉴权方式：导出 `OCTO_BOT_TOKEN`(`bf_…`)与 `OCTO_API_BASE_URL`。通用标志与输出约定见 [CLI 总览](/zh/reference/octo-cli)。

### `group create`

Create a new group (User Bot only; App Bot rejected) · 🟠 写

```bash
octo-cli group create [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--name` | string |  |  |
| `--data` | json | ✓ | members: JSON payload |
| `--creator` | string |  |  |
| `--space_id` | string |  |  |

---

### `group get`

Get group info · 🟢 读

```bash
octo-cli group get <group_no>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

---

### `group list`

List groups the bot is a member of · 🟢 读

```bash
octo-cli group list [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--space_id` | string |  | optional filter by space |

---

### `group md-get`

Read GROUP.md content · 🟢 读

```bash
octo-cli group md-get <group_no>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

---

### `group md-update`

Update GROUP.md content (bot must be bot_admin) · 🟠 写

```bash
octo-cli group md-update <group_no> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--content` | string | ✓ |  |

---

### `group member-add`

Add members to group (App Bot rejected) · 🟠 写

```bash
octo-cli group member-add <group_no> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--data` | json | ✓ | members: JSON payload |

---

### `group member-remove`

Remove members from group (bot must be bot_admin; App Bot rejected) · 🟠 写

```bash
octo-cli group member-remove <group_no> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--data` | json | ✓ | members: JSON payload |

---

### `group members`

List group members · 🟢 读

```bash
octo-cli group members <group_no>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

---

### `group update`

Update group name/notice (bot must be bot_admin; App Bot rejected) · 🟠 写

```bash
octo-cli group update <group_no> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<group_no>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--name` | string |  |  |
| `--notice` | string |  |  |

