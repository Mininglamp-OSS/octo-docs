---
title: "octo-cli event"
description: "event 领域的 octo-cli 命令 —— 由内嵌的 OpenAPI 规范生成。"
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/event.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 2 条命令, 由以下规范生成： [`octo-cli/internal/registry/specs/event.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/event.json). 每次调用都会在 stdout 输出结构化的 JSON 信封。

### `event ack`

Acknowledge (remove) an event from the queue · 🟠 写

```bash
octo-cli event ack <event_id>
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<event_id>` | 路径参数 |

---

### `event list`

Poll pending events (server-side cursor by event_id) · 🟢 读

```bash
octo-cli event list [flags]
```

Uses POST despite being a read — event_id cursor is carried in the request body.

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--event_id` | integer |  | exclusive lower bound; 0 for initial poll |
| `--limit` | integer |  |  |

