---
title: "octo-cli file"
description: "file 领域的 octo-cli 命令 —— 由内嵌的 OpenAPI 规范生成。"
icon: "terminal"
---

{/*
  DO NOT EDIT — generated by scripts/gen-cli.mjs from the source of truth:
  octo-cli/internal/registry/specs/file.json
  Run `npm run gen:all` after changing the spec. See DESIGN.md §6.
*/}

> 4 条命令, 由以下规范生成： [`octo-cli/internal/registry/specs/file.json`](https://github.com/Mininglamp-OSS/octo-cli/blob/main/internal/registry/specs/file.json). 每次调用都会在 stdout 输出结构化的 JSON 信封。

鉴权方式：导出 `OCTO_BOT_TOKEN`(`bf_…`)与 `OCTO_API_BASE_URL`。通用标志与输出约定见 [CLI 总览](/zh/reference/octo-cli)。

### `file credentials`

Obtain STS temp credentials for direct-to-COS upload · 🟢 读

```bash
octo-cli file credentials [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--filename` | string | ✓ |  |

---

### `file download`

Download a file (302 redirect to presigned URL) · 🟢 读

```bash
octo-cli file download <path> [flags]
```

**位置参数**

| 参数 | 说明 |
|---|---|
| `<path>` | 路径参数 |

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--filename` | string |  | override download filename |

---

### `file presigned`

Get a presigned PUT URL for direct upload · 🟢 读

```bash
octo-cli file presigned [flags]
```

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--filename` | string | ✓ |  |
| `--fileSize` | integer | ✓ | file size in bytes; required, must be a positive integer within the server max |

---

### `file upload`

Upload a file via multipart form · 🟠 写

```bash
octo-cli file upload [flags]
```

multipart/form-data: file=&lt;binary&gt;. Max 100MB.

**标志**

| 标志 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `--type` | string |  | storage bucket subdir |
| `--path` | string |  | override generated path |

